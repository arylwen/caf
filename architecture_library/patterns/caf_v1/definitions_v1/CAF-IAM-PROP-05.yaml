pattern_id: CAF-IAM-PROP-05
name: Explicit Identity Verification
summary: Every access path requires explicit identity verification.
intent: Every access path requires explicit identity verification.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
  refines: CAF-IAM-PROP-01
extended_definition: |
  ### Explicit Identity Verification Pattern

  Every access path requires explicit identity verification.
   nUnder this pattern:

  - All cross-plane calls carry explicit identity and tenant context
  - Identity verification occurs at plane boundaries
  - Identity verification is enforced consistently across synchronous and asynchronous interactions
  \
    - No “internal” or “system” access path bypasses verification

  Internal does not mean trusted.

  \
    ---
source_documents:
- path: architecture_library/22_contura_identity_access_pattern_guide_v1.md
  section_heading: Explicit Identity Verification Pattern
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '### Explicit Identity Verification Pattern Every access path requires explicit identity verification.'
  definition_of_done:
  - "Verify that - All cross-plane calls carry explicit identity and tenant context - Identity verification occurs at plane\
    \ boundaries - Identity verification is enforced consistently across synchronous and asynchronous interactions - No \u201C\
    internal\u201D or \u201Csystem\u201D access path bypasses verification Internal does not mean trusted."
  capabilities_needed:
  - identity_authentication
  - tenancy_context
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: iam.verification_strategy
    summary: Choose among bounded variants for `iam.verification_strategy` (or use `custom`).
    options:
    - option_id: verify_at_ingress_only
      status: defer
      summary: Verify identity at ingress and propagate a verifiable context to internal services.
      notes: ''
    - option_id: verify_at_each_boundary
      status: defer
      summary: Verify identity at each service boundary (defense-in-depth).
      notes: ''
    - option_id: mutual_identity_channel
      status: defer
      summary: Use a mutually authenticated channel for service identity plus request context.
      notes: ''
    - option_id: custom
      status: defer
      summary: Custom (describe your approach; prefer adopting a bounded variant when possible).
      notes: ''
    default_option_id: verify_at_ingress_only
  human_questions:
  - question_id: Q-IAM-VER-01
    question: Which identity verification strategy should be adopted for inter-plane and intra-plane boundaries?
    status: open
    answer: {}
    anchors:
    - caf_pattern: CAF-IAM-PROP-05
    option_set_id: iam.verification_strategy
