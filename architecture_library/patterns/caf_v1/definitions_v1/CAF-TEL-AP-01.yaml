pattern_id: CAF-TEL-AP-01
name: Application Plane Telemetry
summary: '- Per-tenant request rates, latencies, errors - AI agent traces (tools called, steps taken) - Authorization failures
  - Feature usage metrics and workflow outcomes'
intent: '- Per-tenant request rates, latencies, errors - AI agent traces (tools called, steps taken) - Authorization failures
  - Feature usage metrics and workflow outcomes'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: OBS-01
  complements: CTX-01
  complements: POL-01
extended_definition: |
  ### Application Plane Telemetry

  - Per-tenant request rates, latencies, errors  
  - AI agent traces (tools called, steps taken)  
  - Authorization failures  
  - Feature usage metrics and workflow outcomes
source_documents:
- path: architecture_library/20_contura_control_application_data_plane_pattern_guide_v1.md
  section_heading: Application Plane Telemetry
caf:
  layer: caf
  verifiability: plan_ready
  plane: application
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '### Application Plane Telemetry - Per-tenant request rates, latencies, errors - AI agent traces (tools called, steps
    taken) - Authorization failures - Feature usage metrics and workflow outcomes'
  definition_of_done:
  - 'Verify that ### Application Plane Telemetry - Per-tenant request rates, latencies, errors - AI agent traces (tools called,
    steps taken) - Authorization failures - Feature usage metrics and workflow outcomes'
  capabilities_needed:
  - workflow_orchestration
  - observability_emit
  - identity_access
  - cost_governance
  - ai_safety_integration
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
