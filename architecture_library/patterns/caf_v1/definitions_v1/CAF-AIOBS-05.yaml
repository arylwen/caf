pattern_id: CAF-AIOBS-05
name: Multi-Tenant Observability Rules
summary: 'Observability must maintain:'
intent: 'Observability must maintain:'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
extended_definition: '## Multi-Tenant Observability Rules


  Observability must maintain:


  - Tenant isolation

  - Tenant-by-tenant evaluation scoring

  - Prevention of cross-tenant leakage in traces or telemetry

  - Partitioning of logs, metrics, and traces per tenant

  - Controlled aggregation only at anonymized or multi-tenant safe layers


  All telemetry must explicitly encode tenant context.'
source_documents:
- path: architecture_library/11_contura_ai_observability_and_evaluation_specification_v1.md
  section_heading: Multi-Tenant Observability Rules
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '## Multi-Tenant Observability Rules Observability must maintain'
  - '- Tenant isolation - Tenant-by-tenant evaluation scoring - Prevention of cross-tenant leakage in traces or telemetry
    - Partitioning of logs, metrics, and traces per tenant - Controlled aggregation only at anonymized or multi-tenant safe
    layers All telemetry must explicitly encode tenant context.'
  definition_of_done:
  - 'Verify that ## Multi-Tenant Observability Rules Observability must maintain'
  - Verify that - Tenant isolation - Tenant-by-tenant evaluation scoring - Prevention of cross-tenant leakage in traces or
    telemetry - Partitioning of logs, metrics, and traces per tenant - Controlled aggregation only at anonymized or multi-tenant
    safe layers All telemetry must explicitly encode tenant context.
  capabilities_needed:
  - observability_emit
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: ai_observability.multi_tenant_isolation_mode
    description: How observability data is isolated across tenants.
    options:
    - option_id: tenant_tagged
      summary: Shared telemetry store with strict tenant tagging and query enforcement.
      mode: tenant_tagged
    - option_id: partitioned
      summary: Partitioned telemetry per tenant (e.g., per-tenant indexes/buckets).
      mode: partitioned
    - option_id: fully_isolated
      summary: Fully isolated telemetry per tenant (separate stores/credentials).
      mode: fully_isolated
    - option_id: custom
      summary: Custom (fill fields).
      mode: ''
    default_option_id: tenant_tagged
  human_questions:
  - question_id: Q-CAF-AIOBS-05-01
    topic: ai_observability.multi_tenant_isolation_mode
    question: Which multi-tenant observability isolation mode will you adopt?
    status: open
    option_set_id: ai_observability.multi_tenant_isolation_mode
    answer: {}
promotions:
  semantic_inputs:
  - source_path: architecture_library/patterns/caf_v1/definitions_v1/CAF-AIOBS-05.yaml
    scope: cross_cutting
  required_trace_anchors:
  - pattern_id: CAF-AIOBS-05
    anchor_kind: plan_step_archetype
    scope: cross_cutting
