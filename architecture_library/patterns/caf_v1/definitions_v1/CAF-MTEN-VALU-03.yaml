pattern_id: CAF-MTEN-VALU-03
name: Identity & Access Validation
summary: '**Is every identity action evaluated within explicit Tenant Context?**'
intent: '**Is every identity action evaluated within explicit Tenant Context?**'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: '## 3. Identity & Access Validation


  **Is every identity action evaluated within explicit Tenant Context?**


  - PASS: Authorization checks always include tenant scope.

  - FAIL: Identity checks run without tenant awareness.


  **Are user sessions scoped to exactly one tenant?**


  - PASS: Switching tenants issues a new session token.

  - FAIL: A single session spans multiple tenants.


  **Does tenant switching require minting a new tenant-scoped session or token?**


  - PASS: Old tokens are invalidated on tenant switch.

  - FAIL: Tenant switching updates UI state only.


  **Are service identities prevented from bypassing tenant enforcement?**


  - PASS: Services propagate and enforce incoming tenant context.

  - FAIL: Internal services access data without tenant checks.


  **Are agent identities explicitly tenant-scoped?**


  - PASS: Agent tokens include tenant and role claims.

  - FAIL: Agents run under a generic system identity.


  **Is delegated agent identity explicit, constrained, and auditable?**


  - PASS: Delegated roles are approved and logged.

  - FAIL: Agents assume elevated permissions implicitly.


  **Is privilege escalation without explicit policy prohibited?**


  - PASS: Permission requests are denied unless policy allows.

  - FAIL: Agents gain permissions dynamically during execution.


  **Do authorization checks always validate tenant ownership of resources?**


  - PASS: Authorization verifies both role and tenant ownership.

  - FAIL: Role checks ignore tenant boundaries.


  **Are tokens or sessions prohibited from spanning multiple tenants?**


  - PASS: Tokens contain exactly one tenant claim.

  - FAIL: Tokens are valid across multiple tenants.


  ---'
source_documents:
- path: architecture_library/31_contura_multi_tenancy_validation_user_guide_v1.md
  section_heading: 3. Identity & Access Validation
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '**Does tenant switching require minting a new tenant-scoped session or token?** - PASS'
  definition_of_done:
  - 'Verify that ## 3.'
  - Verify that identity & Access Validation **Is every identity action evaluated within explicit Tenant Context?** - PASS
  capabilities_needed:
  - tenancy_context
  - identity_access
  - observability_audit
  - agent_boundary
  - policy_enforcement
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
