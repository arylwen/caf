pattern_id: CAF-IAM-AUTH-04
name: Runtime Policy Consumption
summary: Application Plane systems consume policy intent at runtime without becoming policy authorities.
intent: Application Plane systems consume policy intent at runtime without becoming policy authorities.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
  refines: CAF-IAM-AUTH-01
extended_definition: '### Runtime Policy Consumption Pattern


  Application Plane systems consume policy intent at runtime without becoming policy authorities.


  Under this pattern:


  - Policy updates are propagated from the Control Plane

  - Applications react to policy changes but do not originate them

  - Cached or derived policy state is bounded and refreshable

  - Policy enforcement remains observable and auditable


  Policy consumption is reactive and constrained.


  ---'
source_documents:
- path: architecture_library/22_contura_identity_access_pattern_guide_v1.md
  section_heading: Runtime Policy Consumption Pattern
caf:
  layer: caf
  verifiability: plan_ready
  plane: application
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '### Runtime Policy Consumption Pattern Application Plane systems consume policy intent at runtime without becoming policy
    authorities.'
  - Under this pattern
  definition_of_done:
  - 'Verify that ### Runtime Policy Consumption Pattern Application Plane systems consume policy intent at runtime without
    becoming policy authorities.'
  - Verify that under this pattern
  capabilities_needed:
  - policy_enforcement
  - observability_emit
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: iam.runtime_policy_consumption_mode
    description: How runtime components obtain and apply policy decisions.
    options:
    - option_id: local_compiled_snapshot
      summary: Local compiled policy snapshot loaded/deployed with the service.
      mode: local_snapshot
      cache_ttl_seconds: ''
      rejection_conditions:
      - policy_missing
      - policy_invalid
    - option_id: remote_decision_point
      summary: Remote decision point evaluates requests at runtime.
      mode: remote_pdp
      cache_ttl_seconds: ''
      rejection_conditions:
      - pdp_unavailable
      - request_unverifiable
    - option_id: cached_remote_decisions
      summary: Remote decision point with local caching of decisions/policy.
      mode: cached_pdp
      cache_ttl_seconds: ''
      rejection_conditions:
      - cache_invalid
      - pdp_unavailable
    - option_id: custom
      summary: Custom (fill fields).
      mode: ''
      cache_ttl_seconds: ''
      rejection_conditions: []
    default_option_id: local_compiled_snapshot
  human_questions:
  - question_key: runtime_policy_consumption
    question_id: Q-IAM-RUNTIME-01
    question: How does the runtime consume policy (local snapshot, remote decision point, cached remote)?
    description: Select the runtime policy consumption model.
    option_set_id: iam.runtime_policy_consumption_mode
    specialization_notes:
    - Warranted when policy gating is required for AP operations.
    - If latency/availability constraints exist, record them as anchors and prefer consistent behavior on failures.
    anchors:
    - caf_pattern: CAF-IAM-AUTH-04
