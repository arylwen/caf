# Companion Repository (Scaffold)

Instance: `{{INSTANCE_NAME}}`
Profile: `{{PROFILE_VERSION}}`
Target: `{{COMPANION_REPO_TARGET}}`
Created: {{DATE_YYYY_MM_DD}}

This repository is a **bounded Phase 8 scaffold**.

Codex: see `AGENTS.md` for repo-level instructions.

Rules:
- No runnable production code is generated.
- No vendor/provider selection is performed here.
- Outputs must remain within the lifecycle constraints pinned in:  `reference_architectures/{{INSTANCE_NAME}}/spec/guardrails/profile_parameters.yaml`

This scaffold exists to hold downstream build artifacts once Phase 8 gates are enabled.
