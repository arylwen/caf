pattern_id: CAF-EXE-01
name: Contura Enforcement Chain
summary: 'Plain-text flow:'
intent: 'Plain-text flow:'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
extended_definition: '## The Contura Enforcement Chain


  Plain-text flow:


  Normative requirements (CAF, governance, patterns)

  → ADR decision(s) and scope declaration

  → Canonical checklist IDs

  → Executable artifacts (schema, checks catalog, policy rules)

  → CI evaluation (pre-merge, pre-deploy)

  → Runtime gates (Safety Gates, policy enforcement, quota enforcement)

  → Telemetry and audits (observability spec)

  → Incidents mapped back to checklist IDs (incident classification)

  → Updates to docs, checks, policies, and ADRs'
source_documents:
- path: architecture_library/60_executable_architecture_overview_v1.md
  section_heading: The Contura Enforcement Chain
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '## The Contura Enforcement Chain Plain-text flow'
  - Normative requirements (CAF, governance, patterns) → ADR decision(s) and scope declaration → Canonical checklist IDs → Executable artifacts (schema, checks catalog, policy rules) → CI evaluation (pre-merge, pre-deploy) → Runtime gates (Safety Gates, policy enforcement, quota enforcement) → Telemetry and audits (observability spec) → Incidents mapped back to checklist IDs (incident classification) → Updates to docs, checks, policies, and ADRs
  definition_of_done:
  - 'Verify that ## The Contura Enforcement Chain Plain-text flow'
  - Verify that normative requirements (CAF, governance, patterns) → ADR decision(s) and scope declaration → Canonical checklist IDs → Executable artifacts (schema, checks catalog, policy rules) → CI evaluation (pre-merge, pre-deploy) → Runtime gates (Safety Gates, policy enforcement, quota enforcement) → Telemetry and audits (observability spec) → Incidents mapped back to checklist IDs (incident classification) → Updates to docs, checks, policies, and ADRs
  capabilities_needed:
  - canonical_paths
  - validation_gates
  - adr_integration
  - artifact_catalog
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: process_contract
