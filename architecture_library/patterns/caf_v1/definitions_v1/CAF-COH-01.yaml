pattern_id: CAF-COH-01
name: Terminology Consistency
summary: All documents use glossary-defined terms exclusively No document uses “global user” Identity is never described as authority Tenant Context is always explicit and immutable Workspace is never used as a governance boun...
intent: All documents use glossary-defined terms exclusively No document uses “global user” Identity is never described as authority Tenant Context is always explicit and immutable Workspace is never used as a governance boun...
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: OBS-01
  complements: CTX-01
  complements: POL-01
extended_definition: '## 1. Terminology Consistency


  - [ ] All documents use glossary-defined terms exclusively

  - [ ] No document uses “global user”

  - [ ] Identity is never described as authority

  - [ ] Tenant Context is always explicit and immutable

  - [ ] Workspace is never used as a governance boundary


  ---'
source_documents:
- path: architecture_library/45_contura_library_coherence_audit_checklist_v1.md
  section_heading: 1. Terminology Consistency
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - Terminology Consistency - [ ] All documents use glossary-defined terms exclusively - [ ] No document uses “global user” - [ ] Identity is never described as authority - [ ] Tenant Context is always explicit and immutable - [ ] Workspace is never used as a governance boundary ---
  definition_of_done:
  - Verify that terminology Consistency - [ ] All documents use glossary-defined terms exclusively - [ ] No document uses “global user” - [ ] Identity is never described as authority - [ ] Tenant Context is always explicit and immutable - [ ] Workspace is never used as a governance boundary ---
  capabilities_needed:
  - terminology_lint
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  option_sets:
  - option_set_id: coherence.terminology_enforcement_mode
    summary: Choose how terminology consistency is enforced across CAF artifacts.
    options:
    - option_id: manual_review
      summary: Manual review only (no automated lint).
      enforcement_mode: manual_review
      notes: ''
    - option_id: automated_lint_warn
      summary: Automated terminology lint with warnings (does not block).
      enforcement_mode: automated_lint_warn
      notes: ''
    - option_id: automated_lint_fail_closed
      summary: Automated terminology lint that fails closed on violations.
      enforcement_mode: automated_lint_fail_closed
      notes: ''
    default_option_id: automated_lint_fail_closed
  human_questions:
  - question_key: terminology_enforcement_mode
    question_id: Q-CAF-COH-01-01
    question: How should terminology consistency be enforced across CAF artifacts?
    description: Choose whether to rely on manual review, warnings, or fail-closed terminology linting.
    option_set_id: coherence.terminology_enforcement_mode
    anchors:
    - caf_pattern: CAF-COH-01
  kind: decision_pattern