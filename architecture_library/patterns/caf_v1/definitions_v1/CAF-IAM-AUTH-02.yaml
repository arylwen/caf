pattern_id: CAF-IAM-AUTH-02
name: Delegated Authorization Enforcement
summary: Application Plane systems enforce authorization decisions using **delegated policy intent**.
intent: Application Plane systems enforce authorization decisions using **delegated policy intent**.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
  refines: CAF-IAM-AUTH-01
extended_definition: '### Delegated Authorization Enforcement Pattern


  Application Plane systems enforce authorization decisions using **delegated policy intent**.


  Under this pattern:


  - Authorization checks are applied to tenant workflows and APIs

  - Policy intent is sourced from the Control Plane

  - Enforcement logic does not redefine or reinterpret policy semantics

  - Authorization decisions are scoped to tenant and workflow context


  Applications enforce **what is permitted**, not **what policy means**.


  ---'
source_documents:
- path: architecture_library/22_contura_identity_access_pattern_guide_v1.md
  section_heading: Delegated Authorization Enforcement Pattern
caf:
  layer: caf
  verifiability: plan_ready
  plane: application
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '### Delegated Authorization Enforcement Pattern Application Plane systems enforce authorization decisions using **delegated
    policy intent**.'
  - Under this pattern
  definition_of_done:
  - 'Verify that ### Delegated Authorization Enforcement Pattern Application Plane systems enforce authorization decisions
    using **delegated policy intent**.'
  - Verify that under this pattern
  capabilities_needed:
  - identity_authorization
  - policy_enforcement
  - tenancy_context
  - delegation_boundary
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: iam.authorization_enforcement_mode
    description: How authorization decisions are evaluated/enforced across services.
    options:
    - option_id: central_pdp
      summary: Central policy decision point; services call PDP for decisions.
      enforcement_mode: central_pdp
      notes: ''
    - option_id: embedded_library
      summary: Embedded policy evaluator/library in each service.
      enforcement_mode: embedded_library
      notes: ''
    - option_id: gateway_or_sidecar
      summary: Enforce at gateway/sidecar for coarse checks; services enforce fine-grained.
      enforcement_mode: gateway_or_sidecar
      notes: ''
    - option_id: custom
      summary: Custom (fill fields).
      enforcement_mode: ''
      notes: ''
    default_option_id: central_pdp
  human_questions:
  - question_id: Q-IAM-AUTHORIZATION_ENFORCEMENT_MODE-01
    question: Select an option for iam.authorization_enforcement_mode and fill any required fields.
    option_set_id: iam.authorization_enforcement_mode
    status: open
    answer: {}
