pattern_id: CAF-WF-CP-01
name: 'Control Plane: Orchestration Only'
summary: '- Control Plane acts as a **central orchestrator** of platform workflows: - Tenant provisioning and offboarding
  - Resource scaling and migration - Policy rollout and rollback - CP may use **events or message buses** t...'
intent: '- Control Plane acts as a **central orchestrator** of platform workflows'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: OBS-01
  complements: CTX-01
  complements: POL-01
extended_definition: |
  ### Control Plane: Orchestration Only

  - Control Plane acts as a **central orchestrator** of platform workflows:  
    - Tenant provisioning and offboarding  
    - Resource scaling and migration  
    - Policy rollout and rollback  
  - CP may use **events or message buses** to communicate with AP and DP, but these events are part of an **orchestrated workflow**, not free-form choreography.  
  - Example:  
    - “TenantCreated” event emitted by CP → AP bootstrap service consumes event and initializes tenant data. CP remains the **source of truth**.
source_documents:
- path: architecture_library/20_contura_control_application_data_plane_pattern_guide_v1.md
  section_heading: 'Control Plane: Orchestration Only'
caf:
  layer: caf
  verifiability: plan_ready
  plane: control
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '- Tenant provisioning and offboarding - Resource scaling and migration - Policy rollout and rollback - CP may use **events
    or message buses** to communicate with AP and DP, but these events are part of an **orchestrated workflow**, not free-form
    choreography.'
  definition_of_done:
  - 'Verify that ### Control Plane'
  - Verify that orchestration Only - Control Plane acts as a **central orchestrator** of platform workflows
  capabilities_needed:
  - workflow_orchestration
  - internal_choreography
  - policy_enforcement
  - ai_safety_integration
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
