pattern_id: CAF-MTEN-COST-05
name: Graceful Degradation Patterns
summary: 'When limits are reached, systems SHOULD:'
intent: 'When limits are reached, systems SHOULD:'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: '#### 5.6.7 Graceful Degradation Patterns


  When limits are reached, systems SHOULD:


  - Degrade gracefully where possible

  - Provide clear feedback

  - Avoid partial or corrupt outputs


  Examples:


  - Reduce inference depth

  - Switch to cheaper models

  - Pause agent execution

  - Require explicit user confirmation to continue


  Silent degradation without observability is forbidden.


  ---'
source_documents:
- path: architecture_library/21_contura_multi_tenancy_patterns_v1.md
  section_heading: 5.6.7 Graceful Degradation Patterns
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '#### 5.6.7 Graceful Degradation Patterns When limits are reached, systems SHOULD'
  - '- Reduce inference depth - Switch to cheaper models - Pause agent execution - Require explicit user confirmation to continue
    Silent degradation without observability is forbidden.'
  definition_of_done:
  - 'Verify that #### 5.6.7 Graceful Degradation Patterns When limits are reached, systems SHOULD'
  capabilities_needed:
  - observability_emit
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: cost_enforcement.degradation_strategy
    description: How the system behaves when budget/rate/capacity limits are reached.
    options:
    - option_id: hard_deny
      summary: Hard deny new work once limit exceeded.
      strategy: hard_deny
      notes: ''
    - option_id: degrade_features
      summary: Degrade features/quality levels to stay within limits.
      strategy: degrade_features
      notes: ''
    - option_id: queue_or_delay
      summary: Queue/delay work with backpressure until capacity/budget recovers.
      strategy: queue_or_delay
      notes: ''
    - option_id: custom
      summary: Custom (fill fields).
      strategy: ''
      notes: ''
    default_option_id: hard_deny
  human_questions:
  - question_key: auto_cost_enforcement_degradation_strategy
    question_id: Q-CAF-MTEN-COST-05-AUTO-01
    question: How the system behaves when budget/rate/capacity limits are reached.
    description: How the system behaves when budget/rate/capacity limits are reached.
    option_set_id: cost_enforcement.degradation_strategy
    specialization_notes:
    - 'Auto-generated: this pattern declares option_sets but lacked caf.human_questions.'
    - Refine or replace this question text and specialization notes in the library if needed.
    anchors:
    - caf_pattern: CAF-MTEN-COST-05
