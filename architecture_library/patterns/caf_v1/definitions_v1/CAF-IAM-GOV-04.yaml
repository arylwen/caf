pattern_id: CAF-IAM-GOV-04
name: Policy Intent Definition
summary: The Control Plane defines **identity-related policy intent**, which downstream systems enforce.
intent: The Control Plane defines **identity-related policy intent**, which downstream systems enforce.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  refines: CAF-IAM-GOV-01
  complements: CAF-IAM-07
  complements: CAF-POL-02
extended_definition: '### Policy Intent Definition Pattern


  The Control Plane defines **identity-related policy intent**, which downstream systems enforce.


  Policy intent may include:


  - role or permission models

  - entitlement boundaries

  - safety or compliance constraints

  - delegation rules for services and agents


  The Control Plane defines **what is allowed**; enforcement details are delegated downstream.


  ---'
source_documents:
- path: architecture_library/22_contura_identity_access_pattern_guide_v1.md
  section_heading: Policy Intent Definition Pattern
caf:
  layer: caf
  verifiability: plan_ready
  plane: control
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '### Policy Intent Definition Pattern The Control Plane defines **identity-related policy intent**, which downstream systems
    enforce.'
  - Policy intent may include
  definition_of_done:
  - 'Verify that ### Policy Intent Definition Pattern The Control Plane defines **identity-related policy intent**, which
    downstream systems enforce.'
  - Verify that policy intent may include
  capabilities_needed:
  - identity_authentication
  - identity_authorization
  - policy_enforcement
  - delegation_boundary
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: iam.policy_intent_model
    description: How policy intent is expressed (role-based, attribute-based, hybrid).
    options:
    - option_id: rbac
      summary: Role-based policy intent (roles map to permissions).
      model: rbac
      notes: ''
    - option_id: abac
      summary: Attribute-based policy intent (attributes drive decisions).
      model: abac
      notes: ''
    - option_id: hybrid
      summary: Hybrid role + attribute intent with explicit precedence rules.
      model: hybrid
      notes: ''
    - option_id: custom
      summary: Custom (fill fields).
      model: ''
      notes: ''
    default_option_id: hybrid
  human_questions:
  - question_key: policy_intent
    question_id: Q-IAM-INTENT-01
    question: What policy intent model is used (RBAC, ABAC, hybrid)?
    description: Select how policy intent is represented for authorization decisions.
    option_set_id: iam.policy_intent_model
    specialization_notes:
    - Warranted whenever authorization/policy gating is in scope.
    - Keep consistent with policy enforcement distribution choices (CAF-IAM-07 / CAF-POL-02).
    anchors:
    - caf_pattern: CAF-IAM-GOV-04
    - caf_pattern: CAF-POL-02
