pattern_id: CAF-AISG-03
name: Required Evaluations for Agents, Tools, Models, and Reasoning Traces
summary: Each category must pass specialized evaluative rules.
intent: Each category must pass specialized evaluative rules.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
extended_definition: |
  ## Required Evaluations for Agents, Tools, Models, and Reasoning Traces

  Each category must pass specialized evaluative rules.

  ### Agents

  - Validate autonomy levels (bounded, semi-autonomous, autonomous-with-approval).  
  \
    - Confirm tool boundary definitions.  
  - Assess planning depth, recursion, and halting conditions.  
  - Enforce explicit HITL at critical risk levels.

  ### Tools

  - Must declare allowed operations, input schemas, and constraints.  
  - Must expose deterministic or bounded behavior.  
  - Must reject malformed or unsafe invocations.  
  - Require explicit tenancy and identity guards.

  ### Models

  - Must declare training lineage and update provenance.  
  - Require evaluation for bias, hallucination rate, factual accuracy, and safety posture.  
  - Must support versioning and rollback.

  ### Reasoning Traces

  - Must be stored or redacted based on policy.  
  - Must be available for audit under observability rules.  
  - Must not be exposed to end users beyond allowed safety thresholds.
source_documents:
- path: architecture_library/10_contura_ai_safety_gate_specification_v1.md
  section_heading: Required Evaluations for Agents, Tools, Models, and Reasoning Traces
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '## Required Evaluations for Agents, Tools, Models, and Reasoning Traces Each category must pass specialized evaluative
    rules.'
  - '### Tools - Must declare allowed operations, input schemas, and constraints.'
  - '- Must expose deterministic or bounded behavior.'
  definition_of_done:
  - 'Verify that ## Required Evaluations for Agents, Tools, Models, and Reasoning Traces Each category must pass specialized
    evaluative rules.'
  - 'Verify that ### Tools - Must declare allowed operations, input schemas, and constraints.'
  - Verify that - Must expose deterministic or bounded behavior.
  - Verify that - Must reject malformed or unsafe invocations.
  - 'Verify that ### Models - Must declare training lineage and update provenance.'
  - Verify that - Must support versioning and rollback.
  - 'Verify that ### Reasoning Traces - Must be stored or redacted based on policy.'
  - Verify that - Must be available for audit under observability rules.
  capabilities_needed:
  - observability_emit
  - compliance_evidence
  - policy_enforcement
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  option_sets:
  - option_set_id: agent.autonomy_level
    summary: Select the intended autonomy level for agentic workflows.
    options:
    - option_id: bounded
      status: defer
      summary: Bounded autonomy (strict tool boundaries; limited planning scope).
    - option_id: semi_autonomous
      status: defer
      summary: Semi-autonomous (multi-step planning with constrained escalation).
    - option_id: autonomous_with_approval
      status: defer
      summary: Autonomous-with-approval (HITL required at critical risk levels).
    - option_id: custom
      status: defer
      summary: Custom (describe autonomy and required constraints).
      notes: ''
    default_option_id: bounded
  human_questions:
  - question_key: agent_autonomy_level
    question_id: Q-AISG-AUTO-01
    question: What autonomy level is intended for agentic workflows?
    description: Select one option; use 'custom' only if none fit.
    option_set_id: agent.autonomy_level
    anchors:
    - caf_pattern: CAF-AISG-03
  kind: decision_pattern
