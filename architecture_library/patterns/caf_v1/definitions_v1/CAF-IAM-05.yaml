pattern_id: CAF-IAM-05
name: Data Plane Access Enforcement Patterns
summary: The Data Plane is responsible for **access enforcement at the data layer**, based on identity and context established
  upstream.
intent: The Data Plane is responsible for **access enforcement at the data layer**, based on identity and context established
  upstream.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
  refines: CAF-IAM-01
extended_definition: |
  ## Data Plane Access Enforcement Patterns

  The Data Plane is responsible for **access enforcement at the data layer**, based on identity and context established upstream.

  Data Plane systems do not authenticate end-users, govern identity, or define authorization policy.  
  They enforce access constraints as **delegated execution**, not as a source of authority.

  The following patterns describe how data access is enforced in Contura-compliant systems.

  \
    ---

  ### Upstream-Established Identity Enforcement Pattern

  All Data Plane access enforcement relies on identity and context established by upstream planes.

  Under this pattern:

  - Identity context is established by the Application and Control Planes
  - The Data Plane trusts identity assertions only from governed upstream sources
  - No authentication of end-users or agents occurs in the Data Plane
  - Identity context is always explicit, never inferred from data

  The Data Plane enforces access, but does not decide *who* an actor is.

  ---

  ### Tenant-Scoped Data Access Pattern

  \
    All data access in the Data Plane is evaluated within explicit tenant scope.

  Under this pattern:

  - Tenant context is mandatory for all data operations
  - Data isolation mechanisms are tenant-aware
  - No data access occurs without tenant attribution
  - Cross-tenant access is permitted only when explicitly governed

  Tenant isolation is enforced structurally at the data layer.

  ---

  ### Least-Privilege Data Access Pattern

  The Data Plane enforces least-privilege access based on delegated authorization.

  Under this pattern:

  - Access is limited to the minimum required for the operation
  \
    - Privileges are scoped by tenant, principal type, and context
  - Broad or implicit access is prohibited
  - Elevated access is explicit, auditable, and time-bounded where applicable

  The Data Plane does not compensate for overly broad upstream authorization.

  ---

  ### Policy-Constrained Execution Pattern

  The Data Plane executes data operations within constraints defined by Control Plane policy intent.

  Under this pattern:

  - Retention, deletion, encryption, and residency rules are enforced as execution constraints
  - Policy intent is received from the Control Plane
  - Data Plane systems do not interpret or redefine policy semantics
  - Policy enforcement outcomes are observable and auditable

  Policy governs *how data may be accessed or retained*, not *what data means*.

  ---

  ### Non-Decision-Making Pattern

  The Data Plane must not act as a decision-making authority for governance, lifecycle, or control.

  Under this pattern:

  - Data state must not trigger lifecycle transitions
  - Anomalies or threshold breaches do not autonomously change access or governance
  \
    - Observations are reported upstream rather than acted upon autonomously
  - No control intent is derived from data contents or metrics

  The Data Plane reports facts; it does not issue decisions.

  ---

  ### Uniform Enforcement for Human and Non-Human Principals

  Data access enforcement applies uniformly to all principal types.

  Under this pattern:

  - Human users, services, and agents are subject to the same access rules
  - Agent and service access is constrained by governed identity scope
  - No special bypass exists for automation or internal services
  - Identity context is always included in access evaluation

  Automation does not weaken data protection guarantees.

  ---

  **Normative statement:**

  \
    > Data Plane systems enforce access based on upstream-governed identity and policy, and must not authenticate principals, define authorization semantics, or originate control or governance decisions.
source_documents:
- path: architecture_library/22_contura_identity_access_pattern_guide_v1.md
  section_heading: Data Plane Access Enforcement Patterns
caf:
  layer: caf
  verifiability: plan_ready
  plane: data
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '- Tenant context is mandatory for all data operations - Data isolation mechanisms are tenant-aware - No data access occurs
    without tenant attribution - Cross-tenant access is permitted only when explicitly governed Tenant isolation is enforced
    structurally at the data layer.'
  - '--- ### Non-Decision-Making Pattern The Data Plane must not act as a decision-making authority for governance, lifecycle,
    or control.'
  - '- Data state must not trigger lifecycle transitions - Anomalies or threshold breaches do not autonomously change access
    or governance - Observations are reported upstream rather than acted upon autonomously - No control intent is derived
    from data contents or metrics The Data Plane reports facts'
  definition_of_done:
  - Verify that - Identity context is established by the Application and Control Planes - The Data Plane trusts identity assertions
    only from governed upstream sources - No authentication of end-users or agents occurs in the Data Plane - Identity context
    is always explicit, never inferred from data The Data Plane enforces access, but does not decide *who* an actor is.
  - Verify that - Access is limited to the minimum required for the operation - Privileges are scoped by tenant, principal
    type, and context - Broad or implicit access is prohibited - Elevated access is explicit, auditable, and time-bounded
    where applicable The Data Plane does not compensate for overly broad upstream authorization.
  - 'Verify that --- ### Non-Decision-Making Pattern The Data Plane must not act as a decision-making authority for governance,
    lifecycle, or control.'
  - Verify that - Data state must not trigger lifecycle transitions - Anomalies or threshold breaches do not autonomously
    change access or governance - Observations are reported upstream rather than acted upon autonomously - No control intent
    is derived from data contents or metrics The Data Plane reports facts
  - Verify that --- **Normative statement:** > Data Plane systems enforce access based on upstream-governed identity and policy,
    and must not authenticate principals, define authorization semantics, or originate control or governance decisions.
  capabilities_needed:
  - authn
  - authz_policy
  - identity_model
  - audit_observability
  - agent_identity
  - control_plane_governance
  - data_plane_enforcement
  - anti_pattern_detection
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: data_plane.access_enforcement_location
    summary: Choose among bounded variants for `data_plane.access_enforcement_location` (or use `custom`).
    options:
    - option_id: service_layer
      status: defer
      summary: Enforce access control in the data-plane service layer before data access.
      notes: ''
    - option_id: storage_layer
      status: defer
      summary: Enforce access control at the storage boundary (e.g., row/tenant scoping).
      notes: ''
    - option_id: defense_in_depth
      status: defer
      summary: Enforce at multiple layers (service + storage) with consistent policy inputs.
      notes: ''
    - option_id: custom
      status: defer
      summary: Custom (describe your approach; prefer adopting a bounded variant when possible).
      notes: ''
    default_option_id: service_layer
  human_questions:
  - question_key: auto_data_plane_access_enforcement_location
    question_id: Q-CAF-IAM-05-AUTO-01
    question: Select an option for data_plane.access_enforcement_location.
    description: ''
    option_set_id: data_plane.access_enforcement_location
    specialization_notes:
    - 'Auto-generated: this pattern declares option_sets but lacked caf.human_questions.'
    - Refine or replace this question text and specialization notes in the library if needed.
    anchors:
    - caf_pattern: CAF-IAM-05
