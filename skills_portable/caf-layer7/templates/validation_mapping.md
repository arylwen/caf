--- skills/caf-layer7/templates/layer_7/validation_mapping/validation_mapping.md ---
# Validation Mapping

Instance: `{{INSTANCE_NAME}}`

This mapping is derived from the pinned Layer 6 inputs and the template catalog. It does not invent checklist IDs; it only includes the template-level mapping references present in the catalog.

| template_id | parameter_id | pinned_value | template_level_mapping_refs |
|---|---|---|---|
{{VALIDATION_MAPPING_ROWS}}
