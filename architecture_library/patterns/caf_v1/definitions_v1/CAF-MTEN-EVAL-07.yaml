pattern_id: CAF-MTEN-EVAL-07
name: Identity & Access (Human, Service, Agent)
summary: Derived from architecture_library/90_multi_tenancy_patterns_evaluation_and_completeness_checklist_v1.md section '7. Identity & Access (Human, Service, Agent)'.
intent: Derived from 7. Identity & Access (Human, Service, Agent).
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: '## 7. Identity & Access (Human, Service, Agent)


  ### 7.1 Identity Separation

  - [ ] User identity is clearly distinguished from tenant identity.

  - [ ] Service identity is clearly defined and scoped.

  - [ ] Agent identity is explicitly addressed.


  ### 7.2 Agent × Multi-Tenancy

  - [ ] Agent identity inheritance rules are clear.

  - [ ] Agent actions are always tenant-scoped.

  - [ ] Delegated agent identities are addressed.

  - [ ] Cross-tenant agent execution is explicitly prohibited.

  - [ ] The guide aligns with the Agent Identity Pattern ADR.


  ---'
source_documents:
- path: architecture_library/90_multi_tenancy_patterns_evaluation_and_completeness_checklist_v1.md
  section_heading: 7. Identity & Access (Human, Service, Agent)
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '## 7.'
  - 'Identity & Access (Human, Service, Agent) ### 7.1 Identity Separation - [ ] User identity is clearly distinguished from tenant identity.'
  definition_of_done:
  - 'Verify that ## 7.'
  - 'Verify that identity & Access (Human, Service, Agent) ### 7.1 Identity Separation - [ ] User identity is clearly distinguished from tenant identity.'
  capabilities_needed:
  - identity_access
  - ai_safety_integration
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: checklist
