pattern_id: CAF-AISG-05
name: Guardrail Expectations
summary: Derived from architecture_library/10_contura_ai_safety_gate_specification_v1.md section 'Guardrail Expectations'.
intent: Derived from Guardrail Expectations.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
extended_definition: |
  ## Guardrail Expectations

  ### Pre-Execution Guardrails

  - Schema validation  
  - Policy checks  
  - Role and identity validation  
  - Prompt and reasoning sanitization  
  - Context isolation (tenant boundaries)

  \
    ### Mid-Execution Guardrails

  - Anomaly detection  
  - Recursion depth limits  
  - Execution time caps  
  - Tool usage monitoring  
  - Output drift detection (semantic deviation)

  ### Post-Execution Guardrails

  - Output classification  
  - Redaction rules  
  - Compliance checks  
  - Consistency checks against user intent  
  - Shadow evaluation (A/B validation)
source_documents:
- path: architecture_library/10_contura_ai_safety_gate_specification_v1.md
  section_heading: Guardrail Expectations
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '## Guardrail Expectations ### Pre-Execution Guardrails - Schema validation - Policy checks - Role and identity validation
    - Prompt and reasoning sanitization - Context isolation (tenant boundaries) ### Mid-Execution Guardrails - Anomaly detection
    - Recursion depth limits - Execution time caps - Tool usage monitoring - Output drift detection (semantic deviation) ###
    Post-Execution Guardrails - Output classification - Redaction rules - Compliance checks - Consistency checks against user
    intent - Shadow evaluation (A/B validation)'
  definition_of_done:
  - 'Verify that ## Guardrail Expectations ### Pre-Execution Guardrails - Schema validation - Policy checks - Role and identity
    validation - Prompt and reasoning sanitization - Context isolation (tenant boundaries) ### Mid-Execution Guardrails -
    Anomaly detection - Recursion depth limits - Execution time caps - Tool usage monitoring - Output drift detection (semantic
    deviation) ### Post-Execution Guardrails - Output classification - Redaction rules - Compliance checks - Consistency checks
    against user intent - Shadow evaluation (A/B validation)'
  capabilities_needed:
  - observability_emit
  - compliance_evidence
  - policy_enforcement
  - drift_detection
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  option_sets:
  - option_set_id: safety_gate.guardrail_focus
    summary: Select the primary guardrail focus for Safety Gate enforcement.
    options:
    - option_id: pre_execution_rules
      status: defer
      summary: Pre-execution rules (static checks and policy gates).
    - option_id: mid_execution_monitors
      status: defer
      summary: Mid-execution monitors (throttles, anomaly detection, interrupts).
    - option_id: post_execution_validators
      status: defer
      summary: Post-execution validators (audits, regressions, evidence).
    - option_id: full_bundle
      status: defer
      summary: Full bundle (pre + mid + post).
    - option_id: custom
      status: defer
      summary: Custom (describe guardrail focus).
      notes: ''
    default_option_id: pre_execution_rules
  human_questions:
  - question_key: safety_gate_guardrail_focus
    question_id: Q-AISG-GUARDRAIL-01
    question: What is the primary guardrail focus for Safety Gates?
    description: Select one option; use 'custom' only if none fit.
    option_set_id: safety_gate.guardrail_focus
    anchors:
    - caf_pattern: CAF-AISG-05
  kind: decision_pattern
