pattern_id: CAF-POL-S08
name: Runtime Policy Evaluation
summary: Policy evaluation is a **deterministic system operation**.
intent: Policy evaluation is a **deterministic system operation**.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
extended_definition: '## 8. Runtime Policy Evaluation


  Policy evaluation is a **deterministic system operation**.


  At runtime:


  - Policy evaluation MUST receive complete context

  - Missing or ambiguous context MUST fail closed

  - Evaluation MUST be repeatable

  - Outcomes MUST be explicit


  Policy evaluation MAY occur:


  - Pre-execution

  - Mid-execution

  - Post-execution


  Evaluation timing MUST be declared by the policy.


  ---'
source_documents:
- path: architecture_library/22_contura_policy_engine_architecture_guide_v1.md
  section_heading: 8. Runtime Policy Evaluation
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '- Policy evaluation MUST receive complete context - Missing or ambiguous context MUST fail closed - Evaluation MUST be
    repeatable - Outcomes MUST be explicit Policy evaluation MAY occur'
  - '- Pre-execution - Mid-execution - Post-execution Evaluation timing MUST be declared by the policy.'
  definition_of_done:
  - Verify that - Policy evaluation MUST receive complete context - Missing or ambiguous context MUST fail closed - Evaluation
    MUST be repeatable - Outcomes MUST be explicit Policy evaluation MAY occur
  - Verify that - Pre-execution - Mid-execution - Post-execution Evaluation timing MUST be declared by the policy.
  capabilities_needed:
  - policy_enforcement
  - ai_safety_integration
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: policy.evaluation_topology
    description: Where policy evaluation executes at runtime.
    options:
    - option_id: centralized_pdp
      summary: Central policy decision point (PDP) service
      topology: central_service
      latency_profile: network
    - option_id: embedded_library
      summary: Embedded policy evaluator/library in the service
      topology: embedded
      latency_profile: local
    - option_id: sidecar_pdp
      summary: Sidecar PDP co-located with service
      topology: sidecar
      latency_profile: local_ipc
    - option_id: gateway_pdp
      summary: Gateway evaluates policy before forwarding
      topology: gateway
      latency_profile: edge
    - option_id: custom
      summary: Custom (fill details; prefer adopting a retrieved option when available)
    default_option_id: centralized_pdp
  human_questions:
  - question_key: policy.evaluation_topology
    question_id: Q-POL-EVAL-TOPO-01
    question: Where should runtime policy evaluation occur?
    description: Choose the evaluation topology and note expected availability/latency constraints.
    option_set_id: policy.evaluation_topology
    specialization_notes:
    - If offline/local execution is needed, prefer embedded or sidecar evaluation.
    anchors:
    - caf_pattern: CAF-POL-S08
