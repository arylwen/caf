pattern_id: CAF-SAFE-CP-01
name: Control Plane Responsibilities (Safety & Guardrails)
summary: '- Define Safety Gate configurations (thresholds, escalation rules). - Register which AI flows require human review.
  - Ensure Safety Gates are applied consistently across all AP services.'
intent: '- Define Safety Gate configurations (thresholds, escalation rules).'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: OBS-01
  complements: CTX-01
  complements: POL-01
extended_definition: |
  ### Control Plane Responsibilities (Safety & Guardrails)

  - Define Safety Gate configurations (thresholds, escalation rules).  
  - Register which AI flows require human review.  
  - Ensure Safety Gates are applied consistently across all AP services.
source_documents:
- path: architecture_library/20_contura_control_application_data_plane_pattern_guide_v1.md
  section_heading: Control Plane Responsibilities (Safety & Guardrails)
caf:
  layer: caf
  verifiability: plan_ready
  plane: control
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '- Register which AI flows require human review.'
  definition_of_done:
  - Verify that - Ensure Safety Gates are applied consistently across all AP services.
  capabilities_needed:
  - safety_gating
  - ai_safety_integration
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: safety.guardrail_ownership_model
    summary: Choose among bounded variants for `safety.guardrail_ownership_model` (or use `custom`).
    options:
    - option_id: cp_owns_policy_ap_executes
      status: defer
      summary: Control plane owns guardrail policy; application plane executes policy decisions.
      notes: ''
    - option_id: shared_policy_service
      status: defer
      summary: Shared safety policy service used by planes; CP manages policy lifecycle.
      notes: ''
    - option_id: ap_local_enforcement_cp_audit
      status: defer
      summary: Application plane enforces locally; CP audits and gates releases based on evidence.
      notes: ''
    - option_id: custom
      status: defer
      summary: Custom (describe your approach; prefer adopting a bounded variant when possible).
      notes: ''
    default_option_id: cp_owns_policy_ap_executes
  human_questions:
  - question_key: auto_safety_guardrail_ownership_model
    question_id: Q-CAF-SAFE-CP-01-AUTO-01
    question: Select an option for safety.guardrail_ownership_model.
    description: ''
    option_set_id: safety.guardrail_ownership_model
    specialization_notes:
    - 'Auto-generated: this pattern declares option_sets but lacked caf.human_questions.'
    - Refine or replace this question text and specialization notes in the library if needed.
    anchors:
    - caf_pattern: CAF-SAFE-CP-01
