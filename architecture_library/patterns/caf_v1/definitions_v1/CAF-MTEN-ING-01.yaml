pattern_id: CAF-MTEN-ING-01
name: Domain / Subdomain-Based Routing
summary: Bind tenant identity via DNS or host-level routing.
intent: Bind tenant identity via DNS or host-level routing.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  refines: CAF-MTEN-01
  complements: CTX-01
  complements: POL-01
  complements: PST-01
extended_definition: |
  ##### A. Domain / Subdomain-Based Routing

  ###### **Domain / Subdomain-Based Routing Intent**  

  Bind tenant identity via DNS or host-level routing.

  ###### **Domain / Subdomain-Based Routing: Example**
   n- `tenantA.contura.app`
  - `tenantB.api.contura.app`

  ###### **Domain / Subdomain-Based Routing: Properties**
   n- Tenant identity resolved before application logic
  - Naturally compatible with CDNs and edge routing
  - Strong user mental model

  ###### **Domain / Subdomain-Based Routing: Normative Requirements**

  - Hostname MUST map to exactly one Tenant
  - Host-based tenant claims MUST NOT be overridden by headers or payloads
  - TLS configuration MUST prevent domain hijacking

  ###### **Domain / Subdomain-Based Routing: Failure Modes**

  - Shared wildcard domains without validation
  - Internal services trusting Host headers without sanitization

  ---
source_documents:
- path: architecture_library/21_contura_multi_tenancy_patterns_v1.md
  section_heading: A. Domain / Subdomain-Based Routing
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: core_patterns_are_normative
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - 'Normative Requirements** - Hostname MUST map to exactly one Tenant - Host-based tenant claims MUST NOT be overridden
    by headers or payloads - TLS configuration MUST prevent domain hijacking ###### **Domain / Subdomain-Based Routing'
  definition_of_done:
  - 'Verify that normative Requirements** - Hostname MUST map to exactly one Tenant - Host-based tenant claims MUST
    NOT be overridden by headers or payloads - TLS configuration MUST prevent domain hijacking ###### **Domain /
    Subdomain-Based Routing'
  capabilities_needed:
  - tenancy_context
  - tenancy_routing
  - token_claims
  - context_validation
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
