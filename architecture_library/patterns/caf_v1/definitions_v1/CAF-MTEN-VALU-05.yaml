pattern_id: CAF-MTEN-VALU-05
name: Isolation Validation
summary: '**Is the selected isolation mode explicit metadata governed by Control Plane policy?**'
intent: '**Is the selected isolation mode explicit metadata governed by Control Plane policy?**'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: '## 5. Isolation Validation


  **Is the selected isolation mode explicit metadata governed by Control Plane policy?**


  - PASS: Isolation mode is stored and enforced centrally.

  - FAIL: Isolation logic is hard-coded per service.


  **Are data access paths tenant-scoped by construction?**


  - PASS: Queries require tenant filters by default.

  - FAIL: Queries rely on developer discipline.


  **Are cache keys guaranteed to include tenant identifiers?**


  - PASS: Cache keys are prefixed with tenant ID.

  - FAIL: Shared cache keys are reused across tenants.


  **Is cross-tenant data access structurally impossible?**


  - PASS: Enforcement exists at multiple layers.

  - FAIL: Single missing check can leak data.


  **Are hybrid or silo isolation modes enforced without bypassing Tenant Context rules?**


  - PASS: Dedicated infrastructure still enforces tenant context.

  - FAIL: Dedicated stacks bypass application-level checks.


  **Is network isolation treated as supplementary, not authoritative?**


  - PASS: Application checks remain mandatory.

  - FAIL: Network boundaries replace application enforcement.


  **Is isolation strength consistent with declared tenancy mode?**


  - PASS: Observability confirms isolation behavior.

  - FAIL: Actual behavior diverges from declared mode.


  ---'
source_documents:
- path: architecture_library/31_contura_multi_tenancy_validation_user_guide_v1.md
  section_heading: 5. Isolation Validation
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - Queries require tenant filters by default.
  definition_of_done:
  - 'Verify that ## 5.'
  - Verify that isolation Validation **Is the selected isolation mode explicit metadata governed by Control Plane policy?**
    - PASS
  capabilities_needed:
  - tenancy_context
  - identity_access
  - tenancy_isolation
  - observability_audit
  - policy_enforcement
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: tenancy.isolation_validation_mode
    summary: Choose among bounded variants for `tenancy.isolation_validation_mode` (or use `custom`).
    options:
    - option_id: static_lint_checks
      status: defer
      summary: Static analysis/lint checks for isolation boundary violations.
      notes: ''
    - option_id: integration_tests
      status: defer
      summary: Integration tests that validate tenant isolation invariants end-to-end.
      notes: ''
    - option_id: continuous_audit
      status: defer
      summary: Continuous audit/monitoring to detect isolation regressions over time.
      notes: ''
    - option_id: custom
      status: defer
      summary: Custom (describe your approach; prefer adopting a bounded variant when possible).
      notes: ''
    default_option_id: static_lint_checks
  human_questions:
  - question_key: auto_tenancy_isolation_validation_mode
    question_id: Q-CAF-MTEN-VALU-05-AUTO-01
    question: Select an option for tenancy.isolation_validation_mode.
    description: ''
    option_set_id: tenancy.isolation_validation_mode
    specialization_notes:
    - 'Auto-generated: this pattern declares option_sets but lacked caf.human_questions.'
    - Refine or replace this question text and specialization notes in the library if needed.
    anchors:
    - caf_pattern: CAF-MTEN-VALU-05
