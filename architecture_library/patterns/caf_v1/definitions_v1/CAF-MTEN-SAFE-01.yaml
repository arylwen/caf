pattern_id: CAF-MTEN-SAFE-01
name: Safety Gates as Runtime Control Points
summary: 'Safety Gates are **explicit enforcement points** where execution may be:'
intent: 'Safety Gates are **explicit enforcement points** where execution may be:'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  refines: CAF-MTEN-01
  complements: CTX-01
  complements: POL-01
  complements: PST-01
extended_definition: '#### 5.7.5 Safety Gates as Runtime Control Points


  Safety Gates are **explicit enforcement points** where execution may be:


  - Allowed

  - Modified

  - Paused

  - Blocked

  - Escalated


  Safety Gates MAY occur:


  - Before execution (pre-flight checks)

  - During execution (runtime evaluation)

  - After execution (post-hoc analysis)


  Safety Gates MUST:


  - Receive full Tenant Context

  - Evaluate policy explicitly

  - Emit tenant-scoped audit and telemetry

  - Be deterministic and explainable


  Safety Gates MUST NOT rely on best-effort or implicit checks.


  ---'
source_documents:
- path: architecture_library/21_contura_multi_tenancy_patterns_v1.md
  section_heading: 5.7.5 Safety Gates as Runtime Control Points
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '- Before execution (pre-flight checks) - During execution (runtime evaluation) - After execution (post-hoc analysis)
    Safety Gates MUST'
  - '- Receive full Tenant Context - Evaluate policy explicitly - Emit tenant-scoped audit and telemetry - Be deterministic
    and explainable Safety Gates MUST NOT rely on best-effort or implicit checks.'
  definition_of_done:
  - Verify that - Before execution (pre-flight checks) - During execution (runtime evaluation) - After execution (post-hoc
    analysis) Safety Gates MUST
  - Verify that - Receive full Tenant Context - Evaluate policy explicitly - Emit tenant-scoped audit and telemetry - Be deterministic
    and explainable Safety Gates MUST NOT rely on best-effort or implicit checks.
  capabilities_needed:
  - tenancy_context
  - audit_trail
  - observability_emit
  - safety_gating
  - anti_pattern_detection
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
