pattern_id: CAF-MTEN-VALU-10
name: Anti-Pattern Detection Checklist
summary: '**Is any tenant context implicit or inferred?**'
intent: '**Is any tenant context implicit or inferred?**'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: |
  ## 10. Anti-Pattern Detection Checklist

  **Is any tenant context implicit or inferred?**

  - PASS: Tenant context is always explicit.
  - FAIL: “Current tenant” globals exist.

  **Are workspaces, projects, or folders acting as tenants?**

  - PASS: Workspaces are non-governing.
  - FAIL: Workspaces control billing or auth.

  \
    **Are billing plans or cost tiers used for authorization or isolation?**

  - PASS: Billing informs policy only.
  - FAIL: Billing gates access.

  **Is any identity operating without tenant scope?**

  - PASS: All identities are tenant-scoped.
  \
    - FAIL: Global admin identities exist.

  **Are there multi-tenant sessions or tokens?**

  - PASS: Tokens are single-tenant.
  \
    - FAIL: Tokens span tenants.

  **Are agents executing without explicit identity or budgets?**

  - PASS: Agents have scoped identity and limits.
  - FAIL: Agents run unchecked.

  **Are background jobs running without tenant binding?**

  - PASS: Jobs validate tenant context.
  - FAIL: Jobs infer tenant later.

  **Is network isolation treated as sufficient on its own?**

  - PASS: Application checks remain mandatory.
  - FAIL: Network boundaries replace logic.

  **Are enterprise customers handled via custom code paths?**

  - PASS: Enterprise is a mode, not a fork.
  - FAIL: Special-case code exists.
   n**Is governance deferred or introduced late in the lifecycle?**

  - PASS: Governance exists from v1.
  - FAIL: Governance is postponed.
source_documents:
- path: architecture_library/31_contura_multi_tenancy_validation_user_guide_v1.md
  section_heading: 10. Anti-Pattern Detection Checklist
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '**Are billing plans or cost tiers used for authorization or isolation?** - PASS'
  definition_of_done:
  - Verify that **Are there multi-tenant sessions or tokens?** - PASS
  capabilities_needed:
  - tenancy_context
  - identity_access
  - tenant_lifecycle
  - tenancy_isolation
  - metering_usage
  - agent_boundary
  - enterprise_compliance
  - policy_enforcement
  - anti_pattern_detection
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
