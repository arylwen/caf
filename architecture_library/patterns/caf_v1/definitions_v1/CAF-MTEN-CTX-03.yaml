pattern_id: CAF-MTEN-CTX-03
name: Context Enforcement Patterns
summary: Enforcement ensures Tenant Context is **actually used**, not just carried.
intent: Enforcement ensures Tenant Context is **actually used**, not just carried.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: '#### 5.3.6 Context Enforcement Patterns


  Enforcement ensures Tenant Context is **actually used**, not just carried.


  Enforcement points include:


  - Authorization checks

  - Data access layers

  - Cache key construction

  - Tool access policies

  - AI retrieval filters

  - Observability tagging


  ##### **Context Enforcement Patterns Normative Requirements**


  - All data access MUST be tenant-scoped by construction

  - Cache keys MUST include tenant identifiers

  - Tool and agent permissions MUST evaluate tenant context

  - AI systems MUST apply tenant filters at retrieval time


  Enforcement MUST occur at multiple layers (defense in depth).


  ---'
source_documents:
- path: architecture_library/21_contura_multi_tenancy_patterns_v1.md
  section_heading: 5.3.6 Context Enforcement Patterns
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '#### 5.3.6 Context Enforcement Patterns Enforcement ensures Tenant Context is **actually used**, not just carried.'
  - '- Authorization checks - Data access layers - Cache key construction - Tool access policies - AI retrieval filters -
    Observability tagging ##### **Context Enforcement Patterns Normative Requirements** - All data access MUST be tenant-scoped
    by construction - Cache keys MUST include tenant identifiers - Tool and agent permissions MUST evaluate tenant context
    - AI systems MUST apply tenant filters at retrieval time Enforcement MUST occur at multiple layers (defense in depth).'
  definition_of_done:
  - 'Verify that - Authorization checks - Data access layers - Cache key construction - Tool access policies - AI retrieval
    filters - Observability tagging ##### **Context Enforcement Patterns Normative Requirements** - All data access MUST be
    tenant-scoped by construction - Cache keys MUST include tenant identifiers - Tool and agent permissions MUST evaluate
    tenant context - AI systems MUST apply tenant filters at retrieval time Enforcement MUST occur at multiple layers (defense
    in depth).'
  capabilities_needed:
  - tenancy_context
  - observability_emit
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: tenant_context.enforcement_pattern
    description: Where tenant context is enforced and re-verified.
    options:
    - option_id: ingress_only
      summary: Enforce tenant context only at ingress boundaries.
      pattern: ingress_only
      notes: ''
    - option_id: defense_in_depth
      summary: Re-verify/enforce at each internal boundary (service-to-service).
      pattern: defense_in_depth
      notes: ''
    - option_id: capability_scoped
      summary: Enforce per capability boundary; stronger checks for sensitive ops.
      pattern: capability_scoped
      notes: ''
    - option_id: custom
      summary: Custom (fill fields).
      pattern: ''
      notes: ''
    default_option_id: ingress_only
  human_questions:
  - question_key: auto_tenant_context_enforcement_pattern
    question_id: Q-CAF-MTEN-CTX-03-AUTO-01
    question: Where tenant context is enforced and re-verified.
    description: Where tenant context is enforced and re-verified.
    option_set_id: tenant_context.enforcement_pattern
    specialization_notes:
    - 'Auto-generated: this pattern declares option_sets but lacked caf.human_questions.'
    - Refine or replace this question text and specialization notes in the library if needed.
    anchors:
    - caf_pattern: CAF-MTEN-CTX-03
