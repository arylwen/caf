pattern_id: CAF-MTEN-ISO-02
name: Pooled Compute + Partitioned Data (Bridge Isolation)
summary: '**Pooled Compute + Partitioned Data: Intent** Retain shared application infrastructure while increasing isolation
  and lifecycle flexibility by partitioning tenant data.'
intent: '**Pooled Compute + Partitioned Data'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: |
  #### 5.2.2 Pooled Compute + Partitioned Data (Bridge Isolation)

  **Pooled Compute + Partitioned Data: Intent**  
  Retain shared application infrastructure while increasing isolation and lifecycle flexibility by partitioning tenant data.

  ##### **Pooled Compute + Partitioned Data: Applies When**

  - A single shared database becomes a bottleneck
  \
    - Backup/restore per tenant becomes necessary
  - Data residency needs begin to emerge
  - Some tenants outgrow pooled data constraints

  ##### **Pooled Compute + Partitioned Data: Forces & Trade-offs**

  - optimizes: + Stronger data-level blast radius containment
  - optimizes: + Enables tenant-specific retention and deletion workflows
  - optimizes: + Supports tiering and gradual enterprise upgrades
  - sacrifices: − Adds routing complexity (data location lookup)
  - sacrifices: − Requires connection management discipline

  ##### **Pooled Compute + Partitioned Data: Normative Requirements**
   n- Data location MUST be resolved from trusted metadata (not client input)
  - Tenant Context MUST be bound to data access paths
  - Tenant migrations MUST be supported as explicit workflows
  - Tenant placement MUST be auditable

  ##### **Pooled Compute + Partitioned Data: Common Variants**

  - Schema-per-tenant
  - DB-per-tenant within shared cluster/pool
  - Shard-per-cohort (groups of tenants per shard)

  ##### **Pooled Compute + Partitioned Data: Failure Modes**

  - Wrong shard routing (read/write divergence)
  - Partial migrations leaving orphaned data
  - Inconsistent enforcement across mixed storage systems

  ##### **Pooled Compute + Partitioned Data: Safe Composition**

  - Strongly recommended with:
    - Tenant Directory / Placement Catalog patterns (Section 5.6)
    - Lifecycle migration playbooks (Section 5.6)
    - Tenant-aware observability tags in all data paths (Section 5.8)

  ##### **Pooled Compute + Partitioned Data: Evolution Path**

  - Extend to hybrid by:
    - carving out “whale” tenants into dedicated clusters
    - adding regional deployments for residency
    - isolating specific workloads into sandbox execution

  ---
source_documents:
- path: architecture_library/21_contura_multi_tenancy_patterns_v1.md
  section_heading: 5.2.2 Pooled Compute + Partitioned Data (Bridge Isolation)
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - 'Applies When** - A single shared database becomes a bottleneck - Backup/restore per tenant becomes necessary - Data residency
    needs begin to emerge - Some tenants outgrow pooled data constraints ##### **Pooled Compute + Partitioned Data'
  - "\u2212 Requires connection management discipline ##### **Pooled Compute + Partitioned Data"
  - 'Normative Requirements** - Data location MUST be resolved from trusted metadata (not client input) - Tenant Context MUST
    be bound to data access paths - Tenant migrations MUST be supported as explicit workflows - Tenant placement MUST be auditable
    ##### **Pooled Compute + Partitioned Data'
  definition_of_done:
  - 'Verify that normative Requirements** - Data location MUST be resolved from trusted metadata (not client input) - Tenant
    Context MUST be bound to data access paths - Tenant migrations MUST be supported as explicit workflows - Tenant placement
    MUST be auditable ##### **Pooled Compute + Partitioned Data'
  capabilities_needed:
  - tenancy_context
  - tenancy_isolation
  - tenancy_routing
  - audit_retention
  - observability_emit
  - tenant_migration
  - tenant_deletion_retention
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  option_sets:
  - option_set_id: tenancy.isolation.pooled_compute_partitioned_data.variant
    summary: Select the primary data-partitioning strategy when compute is pooled.
    options:
    - option_id: schema_per_tenant
      status: defer
      summary: Schema-per-tenant (single DB instance/cluster).
    - option_id: db_per_tenant
      status: defer
      summary: DB-per-tenant within a shared cluster/pool.
    - option_id: shard_per_cohort
      status: defer
      summary: Shard-per-cohort (groups of tenants per shard).
    - option_id: custom
      status: defer
      summary: Custom (describe partitioning strategy and routing).
      notes: ''
    default_option_id: schema_per_tenant
  human_questions:
  - question_key: pooled_compute_partitioned_data_variant
    question_id: Q-MTEN-ISO-BRIDGE-01
    question: If using pooled compute + partitioned data, what partitioning variant do you intend?
    description: Select one option; use 'custom' only if none fit.
    option_set_id: tenancy.isolation.pooled_compute_partitioned_data.variant
    anchors:
    - caf_pattern: CAF-MTEN-ISO-02
  kind: decision_pattern
