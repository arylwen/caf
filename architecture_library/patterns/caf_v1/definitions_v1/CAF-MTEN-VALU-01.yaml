pattern_id: CAF-MTEN-VALU-01
name: Global Tenancy Invariants
summary: '**Does every Resource belong to exactly one Tenant?**'
intent: '**Does every Resource belong to exactly one Tenant?**'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: |
  ## 1. Global Tenancy Invariants

  **Does every Resource belong to exactly one Tenant?**

  - PASS: A document record includes a single immutable `tenant_id` set at creation and validated on every access.
  - FAIL: A document can exist without a `tenant_id`, or can be reassigned to a different tenant.

  **Is explicit Tenant Context required for every request, action, and execution?**

  - PASS: Every API handler requires a resolved Tenant Context object before execution.
  - FAIL: Business logic executes assuming a “current tenant” stored in global state.

  **Is implicit or inferred tenant context completely prohibited?**

  - PASS: Tenant identity is derived only from signed tokens or verified domains.
  - FAIL: Tenant is inferred from a resource ID prefix or UI selection alone.

  **Is AccountScope the sole authority for billing, authentication configuration, and compliance policy?**

  - PASS: Entitlements and SSO configuration are loaded only from AccountScope metadata.
  - FAIL: Individual services maintain their own billing or SSO logic.

  **Are Workspaces explicitly prevented from acting as tenant or governance boundaries?**

  - PASS: Workspaces group resources but do not own billing, auth, or compliance settings.
  - FAIL: Workspace admins can configure authentication or billing rules.

  **Are CostCenters prevented from influencing isolation, routing, or authorization?**

  - PASS: CostCenters are used only for reporting and attribution.
  - FAIL: Requests are routed or authorized differently based on CostCenter.
   n**Are tenancy boundaries intentional and high-friction by design?**

  - PASS: Tenant creation requires explicit provisioning and audit.
  - FAIL: Tenants are auto-created implicitly during normal user actions.

  ---
source_documents:
- path: architecture_library/31_contura_multi_tenancy_validation_user_guide_v1.md
  section_heading: 1. Global Tenancy Invariants
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - Every API handler requires a resolved Tenant Context object before execution.
  - CostCenters are used only for reporting and attribution.
  - Tenant creation requires explicit provisioning and audit.
  definition_of_done:
  - Verify that global Tenancy Invariants **Does every Resource belong to exactly one Tenant?** - PASS
  - Verify that **Is explicit Tenant Context required for every request, action, and execution?** - PASS
  capabilities_needed:
  - tenancy_context
  - tenancy_routing
  - identity_access
  - tenant_lifecycle
  - tenancy_isolation
  - metering_usage
  - observability_audit
  - enterprise_compliance
  - policy_enforcement
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
