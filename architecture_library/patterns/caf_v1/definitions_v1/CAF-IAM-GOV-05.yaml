pattern_id: CAF-IAM-GOV-05
name: Identity Lifecycle Governance
summary: The Control Plane governs identity lifecycle transitions.
intent: The Control Plane governs identity lifecycle transitions.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
  refines: CAF-IAM-GOV-01
extended_definition: '### Identity Lifecycle Governance Pattern


  The Control Plane governs identity lifecycle transitions.


  Under this pattern:


  - Identity creation, activation, suspension, and revocation are explicit

  - Lifecycle transitions are auditable and observable

  - Downstream systems react to lifecycle state but do not initiate it

  - Identity lifecycle is decoupled from application runtime state


  This prevents identity drift and orphaned authority.


  ---'
source_documents:
- path: architecture_library/22_contura_identity_access_pattern_guide_v1.md
  section_heading: Identity Lifecycle Governance Pattern
caf:
  layer: caf
  verifiability: plan_ready
  plane: control
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '### Identity Lifecycle Governance Pattern The Control Plane governs identity lifecycle transitions.'
  - Under this pattern
  definition_of_done:
  - 'Verify that ### Identity Lifecycle Governance Pattern The Control Plane governs identity lifecycle transitions.'
  - Verify that under this pattern
  capabilities_needed:
  - identity_authentication
  - observability_emit
  - revocation_propagation
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
