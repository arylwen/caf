pattern_id: CAF-IAM-AUTH-01
name: Authentication Validation
summary: "Application Plane systems validate incoming identities according to Control Plane\u2013governed integration and\
  \ trust rules."
intent: "Application Plane systems validate incoming identities according to Control Plane\u2013governed integration and trust\
  \ rules."
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
  refines: CAF-IAM-01
extended_definition: |
  ### Authentication Validation Pattern

  Application Plane systems validate incoming identities according to Control Plane–governed integration and trust rules.

  Under this pattern:

  - Authentication tokens or credentials are validated against governed trust boundaries
  - Identity context (principal type, tenant, roles) is extracted and verified
  \
    - Invalid or untrusted identities are rejected at the earliest entry point
  - Authentication validation is consistent across all tenant-facing entry paths

  Authentication establishes **who is acting**, but does not determine **what is allowed**.
   n---
source_documents:
- path: architecture_library/22_contura_identity_access_pattern_guide_v1.md
  section_heading: Authentication Validation Pattern
caf:
  layer: caf
  verifiability: plan_ready
  plane: application
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - "### Authentication Validation Pattern Application Plane systems validate incoming identities according to Control Plane\u2013\
    governed integration and trust rules."
  - Under this pattern
  definition_of_done:
  - "Verify that ### Authentication Validation Pattern Application Plane systems validate incoming identities according to\
    \ Control Plane\u2013governed integration and trust rules."
  - Verify that under this pattern
  capabilities_needed:
  - identity_authentication
  - identity_authorization
  - tenancy_context
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: iam.authentication_validation_mode
    description: Where and how authentication tokens/credentials are validated.
    options:
    - option_id: edge_gateway
      summary: Validate at the edge/gateway; upstream trusts validated identity context.
      validation_location: edge_gateway
      notes: ''
    - option_id: service_validation
      summary: Each service validates credentials/tokens locally (shared library).
      validation_location: service_validation
      notes: ''
    - option_id: central_introspection
      summary: Central introspection/verification service is queried for validation.
      validation_location: central_introspection
      notes: ''
    - option_id: custom
      summary: Custom (fill fields).
      validation_location: ''
      notes: ''
    default_option_id: edge_gateway
  human_questions:
  - question_id: Q-IAM-AUTHENTICATION_VALIDATION_MODE-01
    question: Select an option for iam.authentication_validation_mode and fill any required fields.
    option_set_id: iam.authentication_validation_mode
    status: open
    answer: {}
promotions:
  semantic_inputs:
  - source_path: architecture_library/patterns/caf_v1/definitions_v1/CAF-IAM-AUTH-01.yaml
    scope: cross_cutting
  required_trace_anchors:
  - pattern_id: CAF-IAM-AUTH-01
    anchor_kind: plan_step_archetype
    scope: cross_cutting
