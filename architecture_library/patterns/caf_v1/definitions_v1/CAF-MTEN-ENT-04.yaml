pattern_id: CAF-MTEN-ENT-04
name: Isolation Evolution Under Enterprise Modes
summary: Enterprise modes often require **stronger isolation**, but not universally.
intent: Enterprise modes often require **stronger isolation**, but not universally.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: |
  ### 6.4 Isolation Evolution Under Enterprise Modes

  Enterprise modes often require **stronger isolation**, but not universally.

  Patterns include:

  - Promoting a tenant from pooled → hybrid → siloed
  - Isolating specific resources or workloads
  - Introducing dedicated data planes while retaining pooled control planes
  - Applying network-level boundaries (accounts, VPCs, projects) where required

  Normative rules:

  - Isolation mode MUST be explicit metadata
  - Routing MUST be deterministic and auditable
  - Migration MUST be a lifecycle workflow, not an operational shortcut
  - Isolation changes MUST preserve Tenant Context invariants

  Network-level isolation MAY be used to satisfy compliance or due diligence requirements,
  but it **supplements** application-level enforcement—it does not replace it.

  ---
source_documents:
- path: architecture_library/21_contura_multi_tenancy_patterns_v1.md
  section_heading: 6.4 Isolation Evolution Under Enterprise Modes
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '### 6.4 Isolation Evolution Under Enterprise Modes Enterprise modes often require **stronger isolation**, but not universally.'
  - "- Isolation mode MUST be explicit metadata - Routing MUST be deterministic and auditable - Migration MUST be a lifecycle\
    \ workflow, not an operational shortcut - Isolation changes MUST preserve Tenant Context invariants Network-level isolation\
    \ MAY be used to satisfy compliance or due diligence requirements, but it **supplements** application-level enforcement\u2014\
    it does not replace it."
  definition_of_done:
  - "Verify that - Promoting a tenant from pooled \u2192 hybrid \u2192 siloed - Isolating specific resources or workloads\
    \ - Introducing dedicated data planes while retaining pooled control planes - Applying network-level boundaries (accounts,\
    \ VPCs, projects) where required Normative rules"
  - "Verify that - Isolation mode MUST be explicit metadata - Routing MUST be deterministic and auditable - Migration MUST\
    \ be a lifecycle workflow, not an operational shortcut - Isolation changes MUST preserve Tenant Context invariants Network-level\
    \ isolation MAY be used to satisfy compliance or due diligence requirements, but it **supplements** application-level\
    \ enforcement\u2014it does not replace it."
  capabilities_needed:
  - tenancy_context
  - tenancy_routing
  - tenant_lifecycle
  - tenancy_isolation
  - observability_audit
  - enterprise_compliance
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: tenancy.isolation_evolution_strategy
    summary: Choose among bounded variants for `tenancy.isolation_evolution_strategy` (or use `custom`).
    options:
    - option_id: fixed_tier_by_stage
      status: defer
      summary: Use a fixed isolation tier per lifecycle stage (explicit evolution path).
      notes: ''
    - option_id: policy_triggered_upshift
      status: defer
      summary: Evolve isolation tier when compliance/risk policy thresholds trigger.
      notes: ''
    - option_id: per_tenant_override
      status: defer
      summary: Allow per-tenant isolation overrides within guardrails and audit.
      notes: ''
    - option_id: custom
      status: defer
      summary: Custom (describe your approach; prefer adopting a bounded variant when possible).
      notes: ''
    default_option_id: fixed_tier_by_stage
  human_questions:
  - question_key: auto_tenancy_isolation_evolution_strategy
    question_id: Q-CAF-MTEN-ENT-04-AUTO-01
    question: Select an option for tenancy.isolation_evolution_strategy.
    description: ''
    option_set_id: tenancy.isolation_evolution_strategy
    specialization_notes:
    - 'Auto-generated: this pattern declares option_sets but lacked caf.human_questions.'
    - Refine or replace this question text and specialization notes in the library if needed.
    anchors:
    - caf_pattern: CAF-MTEN-ENT-04
