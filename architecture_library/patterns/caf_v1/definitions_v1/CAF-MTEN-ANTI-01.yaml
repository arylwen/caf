pattern_id: CAF-MTEN-ANTI-01
name: Implicit Tenancy
summary: Derived from architecture_library/21_contura_multi_tenancy_patterns_v1.md section '7.1 Implicit Tenancy'.
intent: Derived from 7.1 Implicit Tenancy.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: |
  ### 7.1 Implicit Tenancy

  #### **Implicit Tenancy Description**  

  Tenant identity is inferred rather than explicitly bound.

  Common forms:

  - “Current tenant” stored in global or thread-local state
  - Tenant inferred from resource IDs alone
  - UI-selected tenant assumed to persist across calls
  - Background jobs running without tenant binding

  #### **Implicit Tenancy: Why It Happens**

  - Early prototypes
  - Convenience during rapid iteration
  \
    - Assumption that “the UI will handle it”

  #### **Implicit Tenancy: Why It Fails**

  - Cross-tenant data leakage
  \
    - Undetectable authorization bypass
  - Incorrect cost attribution
  - Non-reproducible bugs

  #### **Implicit Tenancy: Normative Rule**

  > If Tenant Context is not explicit, immutable, and validated, tenancy does not exist.

  Implicit tenancy is the **single most common root cause** of multi-tenant failures.

  ---
source_documents:
- path: architecture_library/21_contura_multi_tenancy_patterns_v1.md
  section_heading: 7.1 Implicit Tenancy
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '### 7.1 Implicit Tenancy #### **Implicit Tenancy Description** Tenant identity is inferred rather than explicitly bound.'
  - Common forms
  definition_of_done:
  - 'Verify that ### 7.1 Implicit Tenancy #### **Implicit Tenancy Description** Tenant identity is inferred rather than explicitly
    bound.'
  - Verify that common forms
  capabilities_needed:
  - tenancy_context
  - identity_access
  - anti_pattern_detection
  option_sets:
  - option_set_id: tenancy.binding_enforcement
    summary: Choose how explicit tenancy binding is enforced and how implicit tenancy is prevented.
    options:
    - option_id: runtime_validation
      summary: Runtime validation of explicit tenant context on every boundary crossing.
      binding_enforcement: runtime_validation
      notes: ''
    - option_id: compile_time_guardrails
      summary: Compile-time or build-time guardrails to prevent missing tenant binding.
      binding_enforcement: compile_time_guardrails
      notes: ''
    - option_id: both
      summary: Combine runtime validation and compile/build guardrails.
      binding_enforcement: both
      notes: ''
    default_option_id: both
  human_questions:
  - question_key: tenancy_binding_enforcement
    question_id: Q-CAF-MTEN-ANTI-01-01
    question: How will implicit tenancy be prevented and explicit tenancy binding be enforced?
    description: Choose runtime validation, build-time guardrails, or both.
    option_set_id: tenancy.binding_enforcement
    anchors:
    - caf_pattern: CAF-MTEN-ANTI-01
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern