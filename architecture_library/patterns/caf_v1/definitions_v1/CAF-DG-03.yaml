pattern_id: CAF-DG-03
name: Data as a First-Class Governed Asset
summary: All data in Contura systems MUST be treated as a **first-class governed asset**.
intent: All data in Contura systems MUST be treated as a **first-class governed asset**.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
extended_definition: '## 3. Data as a First-Class Governed Asset


  All data in Contura systems MUST be treated as a **first-class governed asset**.


  A governed data asset MUST have:


  - Explicit ownership and stewardship

  - Explicit tenant scope

  - Explicit classification and sensitivity

  - Explicit quality expectations

  - Explicit lifecycle state

  - Explicit lineage and provenance

  - Explicit access and usage constraints


  Data MUST NOT be:


  - Implicitly trusted

  - Used outside declared scope

  - Accessed without governance context

  - Mutated without attribution

  - Retained without lifecycle intent


  Ungoverned data usage is an architectural defect.


  ---'
source_documents:
- path: architecture_library/23_contura_data_governance_and_data_quality_standards_v1.md
  section_heading: 3. Data as a First-Class Governed Asset
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - Data as a First-Class Governed Asset All data in Contura systems MUST be treated as a **first-class governed asset**.
  - A governed data asset MUST have
  - '- Explicit ownership and stewardship - Explicit tenant scope - Explicit classification and sensitivity - Explicit quality
    expectations - Explicit lifecycle state - Explicit lineage and provenance - Explicit access and usage constraints Data
    MUST NOT be'
  definition_of_done:
  - Verify that data as a First-Class Governed Asset All data in Contura systems MUST be treated as a **first-class governed
    asset**.
  - Verify that a governed data asset MUST have
  - Verify that - Explicit ownership and stewardship - Explicit tenant scope - Explicit classification and sensitivity - Explicit
    quality expectations - Explicit lifecycle state - Explicit lineage and provenance - Explicit access and usage constraints
    Data MUST NOT be
  capabilities_needed:
  - data_governance
  - data_classification
  - data_quality
  - data_lifecycle
  - data_observability_lineage
  - ai_data_constraints
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
