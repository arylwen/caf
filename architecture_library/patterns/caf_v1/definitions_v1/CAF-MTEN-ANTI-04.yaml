pattern_id: CAF-MTEN-ANTI-04
name: Identity Without Tenant Scope
summary: Derived from architecture_library/21_contura_multi_tenancy_patterns_v1.md section '7.4 Identity Without Tenant Scope'.
intent: Derived from 7.4 Identity Without Tenant Scope.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: |
  ### 7.4 Identity Without Tenant Scope

  #### **Identity Without Tenant Scope Description**

  Identity is treated as global, with tenant scope applied “later”.

  Examples:

  - Global admin users
  - Tokens valid across multiple tenants
  - Agent identities without tenant binding
  - Operator tools without explicit tenant selection
   n#### **Identity Without Tenant Scope: Why It Happens**

  - Misunderstanding of authentication vs authorization
  - Over-trusting identity providers
  - Operator convenience

  #### **Identity Without Tenant Scope: Why It Fails**

  - Privilege escalation
  \
    - Cross-tenant access
  - Audit impossibility
  - Regulatory non-compliance

  #### **Identity Without Tenant Scope Normative Rule**

  > Identity without tenant scope is incomplete and unsafe.

  Every identity action MUST answer:
  “Who is acting, in which tenant, under what authority?”

  ---
source_documents:
- path: architecture_library/21_contura_multi_tenancy_patterns_v1.md
  section_heading: 7.4 Identity Without Tenant Scope
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - Every identity action MUST answer
  definition_of_done:
  - Verify that every identity action MUST answer
  capabilities_needed:
  - tenancy_context
  - audit_trail
  - identity_access
  - agent_boundary
  - enterprise_compliance
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: tenancy.identity_tenant_binding_rule
    summary: Choose among bounded variants for `tenancy.identity_tenant_binding_rule` (or use `custom`).
    options:
    - option_id: require_tenant_on_all_principals
      status: defer
      summary: All principals are tenant-scoped; platform actions use explicit tenant context.
    - option_id: allow_platform_principals_without_tenant
      status: defer
      summary: Platform principals may be non-tenant-scoped; tenant principals must be scoped.
    - option_id: explicit_principal_kind_mixed
      status: defer
      summary: 'Mixed: principal kind determines whether tenant scope is required (explicit taxonomy).'
    - option_id: custom
      status: defer
      summary: Custom (describe your approach; prefer adopting a bounded variant when possible).
      notes: ''
    default_option_id: explicit_principal_kind_mixed
  human_questions:
  - question_id: Q-CAF-MTEN-ANTI-04-01
    topic: tenancy.identity_tenant_binding_rule
    question: Which option best matches your intended choice for `tenancy.identity_tenant_binding_rule` and what constraints/rejection
      conditions apply?
    status: open
    answer: {}
    option_set_id: tenancy.identity_tenant_binding_rule
