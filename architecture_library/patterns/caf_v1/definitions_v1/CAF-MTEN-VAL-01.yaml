pattern_id: CAF-MTEN-VAL-01
name: 1. Global Tenancy Invariants
summary: Does every Resource belong to exactly one Tenant? Is explicit Tenant Context required for every request, action,
  and execution? Is implicit or inferred tenant context completely prohibited? Is AccountScope the sole au...
intent: Does every Resource belong to exactly one Tenant?
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: '## 1. Global Tenancy Invariants


  - [ ] Does every Resource belong to exactly one Tenant?

  - [ ] Is explicit Tenant Context required for every request, action, and execution?

  - [ ] Is implicit or inferred tenant context completely prohibited?

  - [ ] Is AccountScope the sole authority for billing, authentication configuration, and compliance policy?

  - [ ] Are Workspaces explicitly prevented from acting as tenant or governance boundaries?

  - [ ] Are CostCenters prevented from influencing isolation, routing, or authorization?

  - [ ] Are tenancy boundaries intentional and high-friction by design?'
source_documents:
- path: architecture_library/30_contura_multi_tenancy_validation_guide_v1.md
  section_heading: 1. Global Tenancy Invariants
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '## 1.'
  - Global Tenancy Invariants - [ ] Does every Resource belong to exactly one Tenant?
  definition_of_done:
  - Verify that global Tenancy Invariants - [ ] Does every Resource belong to exactly one Tenant?
  - Verify that - [ ] Is explicit Tenant Context required for every request, action, and execution?
  capabilities_needed:
  - tenancy_context
  - identity_access
  - tenancy_isolation
  - enterprise_compliance
  - anti_pattern_detection
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
