pattern_id: CAF-IAM-OBS-01
name: Identity Attribution
summary: Every significant action must be attributable to a governed identity.
intent: Every significant action must be attributable to a governed identity.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
  refines: CAF-IAM-01
extended_definition: '### Identity Attribution Pattern


  Every significant action must be attributable to a governed identity.


  Under this pattern:


  - All actions are associated with a principal type and identifier

  - Tenant context is always included where applicable

  - Actions performed by services or agents retain linkage to their root identity

  - No security-relevant action occurs anonymously


  Attribution is required for correctness, not just compliance.


  ---'
source_documents:
- path: architecture_library/22_contura_identity_access_pattern_guide_v1.md
  section_heading: Identity Attribution Pattern
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '### Identity Attribution Pattern Every significant action must be attributable to a governed identity.'
  definition_of_done:
  - 'Verify that ### Identity Attribution Pattern Every significant action must be attributable to a governed identity.'
  - Verify that - All actions are associated with a principal type and identifier - Tenant context is always included where
    applicable - Actions performed by services or agents retain linkage to their root identity - No security-relevant action
    occurs anonymously Attribution is required for correctness, not just compliance.
  capabilities_needed:
  - identity_authentication
  - tenancy_context
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: iam.attribution_granularity
    description: How identity attribution is represented in logs/events/traces.
    options:
    - option_id: request_only
      summary: Request/correlation id only.
      granularity: request_only
      notes: ''
    - option_id: principal_and_tenant
      summary: Include principal + tenant identifiers.
      granularity: principal_and_tenant
      notes: ''
    - option_id: full_chain
      summary: Include full acting chain (user/service/agent/tool) where applicable.
      granularity: full_chain
      notes: ''
    - option_id: custom
      summary: Custom (fill fields).
      granularity: ''
      notes: ''
    default_option_id: request_only
  human_questions:
  - question_key: auto_iam_attribution_granularity
    question_id: Q-CAF-IAM-OBS-01-AUTO-01
    question: How identity attribution is represented in logs/events/traces.
    description: How identity attribution is represented in logs/events/traces.
    option_set_id: iam.attribution_granularity
    specialization_notes:
    - 'Auto-generated: this pattern declares option_sets but lacked caf.human_questions.'
    - Refine or replace this question text and specialization notes in the library if needed.
    anchors:
    - caf_pattern: CAF-IAM-OBS-01
promotions:
  semantic_inputs:
  - source_path: architecture_library/patterns/caf_v1/definitions_v1/CAF-IAM-OBS-01.yaml
    scope: cross_cutting
  required_trace_anchors:
  - pattern_id: CAF-IAM-OBS-01
    anchor_kind: plan_step_archetype
    scope: cross_cutting
