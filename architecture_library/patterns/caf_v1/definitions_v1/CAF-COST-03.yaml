pattern_id: CAF-COST-03
name: Runtime Cost Enforcement & Interruption
summary: Cost enforcement MUST occur at runtime.
intent: Cost enforcement MUST occur at runtime.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
extended_definition: |
  ## 7. Runtime Cost Enforcement & Interruption

  Cost enforcement MUST occur at runtime.

  ### 7.1 Pre-Execution Enforcement

  Systems MUST reject execution if:

  - Budget is exhausted  
  - Quota is exceeded  
  - Entitlement does not allow the action  

  Fail-fast behavior is preferred.

  ---

  ### 7.2 Mid-Execution Enforcement

  Long-running execution MUST support:

  - Periodic budget checks  
  - Progressive throttling  
  - Safe interruption  

  Execution that cannot be stopped is forbidden.

  ---

  ### 7.3 Post-Execution Enforcement

  Post-execution analysis MUST:

  \
    - Detect anomalies  
  - Emit enforcement and audit events  
  - Feed Safety Gates and observability pipelines  

  ---
source_documents:
- path: architecture_library/12_contura_cost_governance_finops_v1.md
  section_heading: 7. Runtime Cost Enforcement & Interruption
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - Runtime Cost Enforcement & Interruption Cost enforcement MUST occur at runtime.
  - '### 7.1 Pre-Execution Enforcement Systems MUST reject execution if'
  - '--- ### 7.2 Mid-Execution Enforcement Long-running execution MUST support'
  definition_of_done:
  - Verify that runtime Cost Enforcement & Interruption Cost enforcement MUST occur at runtime.
  - 'Verify that ### 7.1 Pre-Execution Enforcement Systems MUST reject execution if'
  - 'Verify that --- ### 7.2 Mid-Execution Enforcement Long-running execution MUST support'
  - 'Verify that --- ### 7.3 Post-Execution Enforcement Post-execution analysis MUST'
  capabilities_needed:
  - safety_gating
  - observability_emit
  - cost_governance
  - compliance_evidence
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  option_sets:
  - option_set_id: cost_enforcement.phase_coverage
    summary: Select the intended coverage across pre-, mid-, and post-execution enforcement.
    options:
    - option_id: pre_execution_only
      status: defer
      summary: Pre-execution only (fail-fast; no long-running interruption).
    - option_id: pre_and_mid_execution
      status: defer
      summary: Pre- and mid-execution enforcement (checks, throttling, safe interruption).
    - option_id: pre_mid_and_post_execution
      status: defer
      summary: Pre-, mid-, and post-execution enforcement (including anomaly/audit events).
    - option_id: custom
      status: defer
      summary: Custom (describe coverage across phases).
      notes: ''
    default_option_id: pre_execution_only
  human_questions:
  - question_key: cost_enforcement_phase_coverage
    question_id: Q-COST-ENF-PHASE-01
    question: What enforcement coverage is required across pre-, mid-, and post-execution?
    description: Select one option; use 'custom' only if none fit.
    option_set_id: cost_enforcement.phase_coverage
    anchors:
    - caf_pattern: CAF-COST-03
  kind: decision_pattern
