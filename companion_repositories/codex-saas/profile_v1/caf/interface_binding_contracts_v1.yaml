# Generated by CAF v0.2.0
schema_version: caf_interface_binding_contracts_v1
instance_name: codex-saas
generated_from:
  task_graph: "reference_architectures/codex-saas/design/playbook/task_graph_v1.yaml"
  abp_pbp_resolution: "reference_architectures/codex-saas/spec/guardrails/abp_pbp_resolution_v1.yaml"
bindings:
  - binding_id: BIND-AP-reports
    binding_kind: required_provided_interface_binding
    plane_scope: AP
    resource_key: reports
    description: "Bind the AP reports consumer to a provider implementation in the assembler boundary."
    required_interface:
      name: ReportsAccessInterface
      description: "Required interface for reports operations in the selected architecture style."
      consumer:
        task_id: TG-30-service-facade-reports
        role_id: application_use_cases
    provider:
      task_id: TG-40-persistence-reports
      role_id: outbound_adapters
      binding_kind: provides_implementation
    assembler:
      task_id: TG-90-runtime-wiring
      role_id: composition_root
      binding_action: bind_consumer_to_provider
  - binding_id: BIND-AP-submissions
    binding_kind: required_provided_interface_binding
    plane_scope: AP
    resource_key: submissions
    description: "Bind the AP submissions consumer to a provider implementation in the assembler boundary."
    required_interface:
      name: SubmissionsAccessInterface
      description: "Required interface for submissions operations in the selected architecture style."
      consumer:
        task_id: TG-30-service-facade-submissions
        role_id: application_use_cases
    provider:
      task_id: TG-40-persistence-submissions
      role_id: outbound_adapters
      binding_kind: provides_implementation
    assembler:
      task_id: TG-90-runtime-wiring
      role_id: composition_root
      binding_action: bind_consumer_to_provider
  - binding_id: BIND-AP-workspaces
    binding_kind: required_provided_interface_binding
    plane_scope: AP
    resource_key: workspaces
    description: "Bind the AP workspaces consumer to a provider implementation in the assembler boundary."
    required_interface:
      name: WorkspacesAccessInterface
      description: "Required interface for workspaces operations in the selected architecture style."
      consumer:
        task_id: TG-30-service-facade-workspaces
        role_id: application_use_cases
    provider:
      task_id: TG-40-persistence-workspaces
      role_id: outbound_adapters
      binding_kind: provides_implementation
    assembler:
      task_id: TG-90-runtime-wiring
      role_id: composition_root
      binding_action: bind_consumer_to_provider
