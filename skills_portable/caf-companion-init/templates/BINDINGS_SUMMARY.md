# Bindings Summary (Stub)

Instance: `{{INSTANCE_NAME}}`
Profile: `{{PROFILE_VERSION}}`
Date: {{DATE_YYYY_MM_DD}}

This file records Phase 8 bindings at a high level.

## Upstream Inputs
- Layer 6 pins: `reference_architectures/{{INSTANCE_NAME}}/spec/playbook/architecture_shape_parameters.yaml`
- Layer 7 derived bundle: `reference_architectures/{{INSTANCE_NAME}}/layer_7/`
- Layer 8 profile parameters: `reference_architectures/{{INSTANCE_NAME}}/spec/guardrails/profile_parameters.yaml`

## Output Target
- `{{COMPANION_REPO_TARGET}}`

## Notes
Populate with concrete bindings once skill execution is enabled.
