pattern_id: CAF-MTEN-ING-02
name: Token-Embedded Tenant Claims
summary: Bind tenant identity via cryptographically signed credentials.
intent: Bind tenant identity via cryptographically signed credentials.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  refines: CAF-MTEN-01
  complements: CTX-01
  complements: POL-01
  complements: PST-01
extended_definition: |
  ##### B. Token-Embedded Tenant Claims

  ###### **Token-Embedded Tenant Claims Intent**  

  Bind tenant identity via cryptographically signed credentials.

  ###### **Token-Embedded Tenant Claims Example**

  - JWT containing `tenant_id`, `workspace_id`, roles

  ###### **Token-Embedded Tenant Claims Properties**

  - Works across web, mobile, API, and agent flows
  - Supports explicit tenant switching
  - Strongly auditable

  ###### **Token-Embedded Tenant Claims Normative Requirements**

  - Tenant claims MUST be issued by a trusted authority
  - Tokens MUST be tenant-scoped (no multi-tenant tokens by default)
  - Token refresh MUST preserve tenant binding

  ###### **Token-Embedded Tenant Claims Failure Modes**

  - Tokens valid across tenants
  - Client-controlled tenant fields outside token claims

  ---
source_documents:
- path: architecture_library/21_contura_multi_tenancy_patterns_v1.md
  section_heading: B. Token-Embedded Tenant Claims
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '###### **Token-Embedded Tenant Claims Example** - JWT containing `tenant_id`, `workspace_id`, roles ###### **Token-Embedded
    Tenant Claims Properties** - Works across web, mobile, API, and agent flows - Supports explicit tenant switching - Strongly
    auditable ###### **Token-Embedded Tenant Claims Normative Requirements** - Tenant claims MUST be issued by a trusted authority
    - Tokens MUST be tenant-scoped (no multi-tenant tokens by default) - Token refresh MUST preserve tenant binding ######
    **Token-Embedded Tenant Claims Failure Modes** - Tokens valid across tenants - Client-controlled tenant fields outside
    token claims ---'
  definition_of_done:
  - 'Verify that ###### **Token-Embedded Tenant Claims Example** - JWT containing `tenant_id`, `workspace_id`, roles ######
    **Token-Embedded Tenant Claims Properties** - Works across web, mobile, API, and agent flows - Supports explicit tenant
    switching - Strongly auditable ###### **Token-Embedded Tenant Claims Normative Requirements** - Tenant claims MUST be
    issued by a trusted authority - Tokens MUST be tenant-scoped (no multi-tenant tokens by default) - Token refresh MUST
    preserve tenant binding ###### **Token-Embedded Tenant Claims Failure Modes** - Tokens valid across tenants - Client-controlled
    tenant fields outside token claims ---'
  capabilities_needed:
  - tenancy_context
  - token_claims
  - audit_retention
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
