pattern_id: CAF-MTEN-VALU-08
name: Agent-Specific Validation
summary: '**Do agents always operate under explicit Tenant Context?**'
intent: '**Do agents always operate under explicit Tenant Context?**'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: '## 8. Agent-Specific Validation


  **Do agents always operate under explicit Tenant Context?**


  - PASS: Agents receive tenant-scoped tokens.

  - FAIL: Agents infer tenant from memory.


  **Is agent identity immutable for the duration of a run?**


  - PASS: Identity cannot change mid-run.

  - FAIL: Agents swap roles dynamically.


  **Are agents prohibited from inferring tenant context from stored state?**


  - PASS: Tenant context is always passed explicitly.

  - FAIL: Agents reuse prior context implicitly.


  **Are agent tool permissions explicitly scoped and enforced?**


  - PASS: Tools validate tenant and role.

  - FAIL: Tools trust agent blindly.


  **Are agent actions fully auditable as first-class principals?**


  - PASS: Agent actions emit audit records.

  - FAIL: Agent actions are logged anonymously.


  **Are agent executions bound to tenant lifecycle state?**


  - PASS: Agents stop on suspension.

  - FAIL: Agents ignore lifecycle changes.


  **Are agents prevented from running for suspended or terminated tenants?**


  - PASS: Execution is blocked.

  - FAIL: Execution continues.


  **Are agent reasoning, tool usage, and cost observable per tenant?**


  - PASS: Observability captures all agent activity.

  - FAIL: Agent internals are opaque.


  ---'
source_documents:
- path: architecture_library/31_contura_multi_tenancy_validation_user_guide_v1.md
  section_heading: 8. Agent-Specific Validation
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '## 8.'
  - Agent-Specific Validation **Do agents always operate under explicit Tenant Context?** - PASS
  definition_of_done:
  - 'Verify that ## 8.'
  - Verify that agent-Specific Validation **Do agents always operate under explicit Tenant Context?** - PASS
  capabilities_needed:
  - tenancy_context
  - identity_access
  - tenant_lifecycle
  - metering_usage
  - observability_audit
  - agent_boundary
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
