pattern_id: CAF-INC-02
name: Primary Incident Classes → Checklist Mapping
summary: Derived from architecture_library/32_contura_multi_tenancy_incident_classification_v1.md section '2. Primary Incident Classes → Checklist Mapping'.
intent: Derived from 2. Primary Incident Classes → Checklist Mapping.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
extended_definition: '## 2. Primary Incident Classes → Checklist Mapping


  ### A. Cross-Tenant Data Read Exposure (SEV-0)

  **Signals**

  - Tenant A can read Tenant B resource/data

  - Retrieval returns cross-tenant documents

  - Cache returns wrong-tenant data


  **Likely root checklist failures**

  - MT-CX-030 (Authz tenant scope)

  - MT-CX-031 (Ownership validation)

  - MT-CX-032 (Tenant-scoped caches/queries/tools)

  - MT-CX-001/002 (Ingress binding/authoritative source)


  **Immediate containment**

  - Fail closed on suspected endpoints

  - Disable or hard-scope retrieval/tooling

  - Purge shared caches; rotate tokens if needed


  ---


  ### B. Cross-Tenant Write/Action (SEV-0)

  **Signals**

  - Mutations applied to another tenant

  - Tool invocation performed actions under wrong tenant


  **Likely root checklist failures**

  - MT-CX-030, MT-CX-031, MT-CX-032

  - MT-CX-021 (Async context loss)

  - MT-CX-041 (Agent delegated identity)

  - MT-CX-003 (Fail closed)


  **Immediate containment**

  - Suspend affected execution paths (agents/jobs)

  - Disable external tool actions; switch to read-only mode


  ---


  ### C. “Out-of-Tenant” Background Execution (SEV-1 → SEV-0 if leakage)

  **Signals**

  - Jobs/consumers running without tenant_id

  - Events missing tenant envelope

  - Agents resume without validated context


  **Likely root checklist failures**

  - MT-CX-021 (Async propagation)

  - MT-CX-020 (Downstream propagation)

  - MT-CX-001 (Binding at ingress)


  **Immediate containment**

  - Pause schedulers/consumers; require tenant context validation on consume


  ---


  ### D. Privilege Escalation / Scope Widening (SEV-1)

  **Signals**

  - “Admin” powers without tenant scope

  - Operator access not scoped/audited

  - Agent obtains broader permissions than delegation


  **Likely root checklist failures**

  - MT-CX-040 (Principal explicit)

  - MT-CX-041 (Delegated identity auditable)

  - MT-CX-042 (Single-tenant tokens/sessions)

  - MT-CX-030 (Authz tenant scope)


  **Immediate containment**

  - Lock down roles; enforce least privilege; add explicit scope selection


  ---


  ### E. Missing/Insufficient Auditability (SEV-1 → SEV-2)

  **Signals**

  - Unable to determine tenant/principal/resource for an action

  - Audit events missing tenant_id

  - Traces cannot reconstruct execution path


  **Likely root checklist failures**

  - MT-CX-050 (Telemetry tenant-scoped)

  - MT-CX-051 (Audit events tenant-scoped)


  **Immediate containment**

  - Increase logging/audit sampling; deploy hotfix to include required fields


  ---


  ### F. Runaway Cost / Agent Loop (SEV-1 → SEV-2)

  **Signals**

  - Sudden spend spike attributed to a tenant/agent

  - Tool fan-out explosion

  - Quotas not enforced during long-running runs


  **Likely root checklist failures**

  - MT-CX-060 (Metering)

  - MT-CX-061 (Runtime entitlements)

  - MT-CX-062 (Bounded cost / stop conditions)


  **Immediate containment**

  - Suspend agents; enforce budget cutoffs; throttle tool calls'
source_documents:
- path: architecture_library/32_contura_multi_tenancy_incident_classification_v1.md
  section_heading: 2. Primary Incident Classes → Checklist Mapping
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - 'rotate tokens if needed --- ### B.'
  - 'require tenant context validation on consume --- ### D.'
  definition_of_done:
  - Verify that missing/Insufficient Auditability (SEV-1 → SEV-2) **Signals** - Unable to determine tenant/principal/resource for an action - Audit events missing tenant_id - Traces cannot reconstruct execution path **Likely root checklist failures** - MT-CX-050 (Telemetry tenant-scoped) - MT-CX-051 (Audit events tenant-scoped) **Immediate containment** - Increase logging/audit sampling
  - 'Verify that deploy hotfix to include required fields --- ### F.'
  capabilities_needed:
  - data_validation
  - ai_data_constraints
  - incident_response
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: checklist
