pattern_id: CAF-WF-AP-01
name: 'Application Plane: Tenant Workflow Orchestration + Internal Choreography'
summary: '- Application Plane orchestrates **tenant business workflows** (e.g., purchase flows, focus space workflows) using
  CP policy and DP data. - AP may use **choreography** between its own services (e.g., event-driven micr...'
intent: '- Application Plane orchestrates **tenant business workflows** (e.g., purchase flows, focus space workflows) using
  CP policy and DP data.'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: OBS-01
  complements: CTX-01
  complements: POL-01
extended_definition: |
  ### Application Plane: Tenant Workflow Orchestration + Internal Choreography

  - Application Plane orchestrates **tenant business workflows** (e.g., purchase flows, focus space workflows) using CP policy and DP data.  
  - AP may use **choreography** between its own services (e.g., event-driven microservices) as long as:  
    - Tenant context is preserved.  
    - Global invariants defined by CP are not violated.
source_documents:
- path: architecture_library/20_contura_control_application_data_plane_pattern_guide_v1.md
  section_heading: 'Application Plane: Tenant Workflow Orchestration + Internal Choreography'
caf:
  layer: caf
  verifiability: plan_ready
  plane: application
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '- AP may use **choreography** between its own services (e.g., event-driven microservices) as long as'
  definition_of_done:
  - 'Verify that ### Application Plane'
  - Verify that tenant Workflow Orchestration + Internal Choreography - Application Plane orchestrates **tenant business workflows**
    (e.g., purchase flows, focus space workflows) using CP policy and DP data.
  capabilities_needed:
  - workflow_orchestration
  - internal_choreography
  - tenancy_context
  - policy_enforcement
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
