pattern_id: CAF-MTEN-COST-01
name: Metering as a First-Class Execution Step
summary: Metering MUST be treated as a mandatory execution step, not a side effect.
intent: Metering MUST be treated as a mandatory execution step, not a side effect.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: '#### 5.6.2 Metering as a First-Class Execution Step


  Metering MUST be treated as a mandatory execution step, not a side effect.


  Canonical metering points include:


  - API request handling

  - Job or workflow execution

  - Agent action or inference step

  - Tool invocation

  - Storage read/write operations


  Each metering event MUST include:


  - Tenant identifier

  - Acting principal (user, agent, service)

  - Resource identifier and type

  - Quantity (time, tokens, bytes, calls)

  - Timestamp


  Metering without tenant context is invalid.


  ---'
source_documents:
- path: architecture_library/21_contura_multi_tenancy_patterns_v1.md
  section_heading: 5.6.2 Metering as a First-Class Execution Step
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '#### 5.6.2 Metering as a First-Class Execution Step Metering MUST be treated as a mandatory execution step, not a side
    effect.'
  - '- API request handling - Job or workflow execution - Agent action or inference step - Tool invocation - Storage read/write
    operations Each metering event MUST include'
  definition_of_done:
  - 'Verify that #### 5.6.2 Metering as a First-Class Execution Step Metering MUST be treated as a mandatory execution step,
    not a side effect.'
  - Verify that - API request handling - Job or workflow execution - Agent action or inference step - Tool invocation - Storage
    read/write operations Each metering event MUST include
  capabilities_needed:
  - tenancy_context
  - token_claims
  - metering_usage
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
