pattern_id: CAF-RIC-02
name: Canonical Phase 7 Directory Structure
summary: Each reference implementation MUST conform to the following directory structure.
intent: Each reference implementation MUST conform to the following directory structure.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
extended_definition: |
  ## Canonical Phase 7 Directory Structure

  Each reference implementation MUST conform to the following directory structure.

  ```ascii

  reference_implementations/
    
    <reference_name>/
    
      README.md
    
      pinned_templates/
        architecture_shape_parameters.yaml
    
      adrs/
        adr_index.md
        adr_*.md
    
      validation_mapping/
        validation_mapping.md

  No additional directories are allowed at Phase 7.

  ```

  ---
source_documents:
- path: architecture_library/70_contura_reference_implementation_structure_and_derivation_contract_v1.md
  section_heading: Canonical Phase 7 Directory Structure
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '## Canonical Phase 7 Directory Structure Each reference implementation MUST conform to the following directory structure.'
  definition_of_done:
  - 'Verify that ## Canonical Phase 7 Directory Structure Each reference implementation MUST conform to the following directory structure.'
  capabilities_needed:
  - canonical_paths
  - validation_gates
  - rails_enforcement
  - adr_integration
  - phase7_structure
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: process_contract
