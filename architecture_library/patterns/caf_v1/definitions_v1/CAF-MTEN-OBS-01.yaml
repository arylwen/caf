pattern_id: CAF-MTEN-OBS-01
name: Observability as an Enforcement Primitive
summary: "Observability is often treated as \u201Cafter-the-fact\u201D telemetry."
intent: "Observability is often treated as \u201Cafter-the-fact\u201D telemetry."
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  refines: CAF-MTEN-01
  complements: CTX-01
  complements: POL-01
  complements: PST-01
extended_definition: |
  #### 5.7.1 Observability as an Enforcement Primitive

  Observability is often treated as “after-the-fact” telemetry.

  In Contura systems, observability is:

  - A prerequisite for safety
  - A requirement for isolation
  - An enforcement signal for lifecycle and cost controls

  Observability MUST enable the system to answer, at any point:

  \
    - Which tenant is involved?
  - Which identity acted?
  - Which resources were accessed?
  - What execution path occurred?
  \
    - What policies were evaluated?
  - What cost and impact resulted?

  Any execution path that cannot answer these questions is unsafe.

  ---
source_documents:
- path: architecture_library/21_contura_multi_tenancy_patterns_v1.md
  section_heading: 5.7.1 Observability as an Enforcement Primitive
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '- A prerequisite for safety - A requirement for isolation - An enforcement signal for lifecycle and cost controls Observability
    MUST enable the system to answer, at any point'
  definition_of_done:
  - Verify that - A prerequisite for safety - A requirement for isolation - An enforcement signal for lifecycle and cost controls
    Observability MUST enable the system to answer, at any point
  capabilities_needed:
  - tenancy_context
  - observability_emit
  - identity_access
  - tenant_lifecycle
  - tenancy_isolation
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
