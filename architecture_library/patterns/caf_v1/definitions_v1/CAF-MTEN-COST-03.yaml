pattern_id: CAF-MTEN-COST-03
name: Enforcement During Execution
summary: Some workloads require **continuous enforcement**.
intent: Some workloads require **continuous enforcement**.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: '#### 5.6.4 Enforcement During Execution


  Some workloads require **continuous enforcement**.


  Examples:


  - Long-running agents

  - Streaming inference

  - Batch jobs with fan-out

  - Recursive or adaptive workflows


  Patterns include:


  - Periodic budget checks

  - Step-level quota evaluation

  - Hard execution cutoffs

  - Progressive throttling


  Execution MUST terminate safely when limits are reached.


  ---'
source_documents:
- path: architecture_library/21_contura_multi_tenancy_patterns_v1.md
  section_heading: 5.6.4 Enforcement During Execution
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '#### 5.6.4 Enforcement During Execution Some workloads require **continuous enforcement**.'
  - '- Periodic budget checks - Step-level quota evaluation - Hard execution cutoffs - Progressive throttling Execution MUST
    terminate safely when limits are reached.'
  definition_of_done:
  - Verify that - Periodic budget checks - Step-level quota evaluation - Hard execution cutoffs - Progressive throttling Execution
    MUST terminate safely when limits are reached.
  capabilities_needed:
  - quota_entitlements
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  option_sets:
  - option_set_id: cost_enforcement.runtime_strategy
    summary: Select the primary runtime enforcement strategy used during execution.
    options:
    - option_id: periodic_budget_checkpoints
      status: defer
      summary: Periodic budget checkpoints during execution.
    - option_id: progressive_throttling
      status: defer
      summary: Progressive throttling as budgets/quotas approach exhaustion.
    - option_id: safe_interruption
      status: defer
      summary: Safe interruption/termination of execution when limits are exceeded.
    - option_id: partial_completion_compensation
      status: defer
      summary: Partial completion with compensation/rollback actions.
    - option_id: custom
      status: defer
      summary: Custom (describe runtime enforcement strategy).
      notes: ''
    default_option_id: periodic_budget_checkpoints
  human_questions:
  - question_key: cost_runtime_enforcement_strategy
    question_id: Q-MTEN-COST-RUNTIME-01
    question: What is the primary runtime cost/usage enforcement strategy during execution?
    description: Select one option; use 'custom' only if none fit.
    option_set_id: cost_enforcement.runtime_strategy
    anchors:
    - caf_pattern: CAF-MTEN-COST-03
  kind: decision_pattern
