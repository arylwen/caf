pattern_id: CAF-MTEN-SAFE-03
name: Blast Radius Detection and Containment
summary: Observability enables **blast radius detection**.
intent: Observability enables **blast radius detection**.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  refines: CAF-MTEN-01
  complements: CTX-01
  complements: POL-01
  complements: PST-01
extended_definition: '#### 5.7.7 Blast Radius Detection and Containment


  Observability enables **blast radius detection**.


  Blast radius indicators include:


  - Sudden cost spikes

  - Abnormal agent behavior

  - Unexpected fan-out

  - Policy violation patterns


  Systems MUST be able to:


  - Attribute blast radius to a tenant

  - Contain impact within that tenant

  - Trigger lifecycle or entitlement controls automatically


  Containment actions MAY include:


  - Throttling

  - Agent suspension

  - Tenant suspension

  - Safety gate escalation


  ---'
source_documents:
- path: architecture_library/21_contura_multi_tenancy_patterns_v1.md
  section_heading: 5.7.7 Blast Radius Detection and Containment
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '- Sudden cost spikes - Abnormal agent behavior - Unexpected fan-out - Policy violation patterns Systems MUST be able
    to'
  definition_of_done:
  - Verify that - Sudden cost spikes - Abnormal agent behavior - Unexpected fan-out - Policy violation patterns Systems MUST
    be able to
  capabilities_needed:
  - tenancy_context
  - observability_emit
  - safety_gating
  - metering_usage
  - tenant_lifecycle
  - agent_boundary
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
