pattern_id: CAF-MTEN-VALU-07
name: Observability, Audit & Safety Validation
summary: '**Do all logs include tenant identifiers?**'
intent: '**Do all logs include tenant identifiers?**'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: '## 7. Observability, Audit & Safety Validation


  **Do all logs include tenant identifiers?**


  - PASS: Logs are tenant-tagged.

  - FAIL: Logs omit tenant context.


  **Do all metrics include tenant attribution?**


  - PASS: Metrics are partitioned per tenant.

  - FAIL: Metrics aggregate across tenants.


  **Do all traces include tenant and principal context?**


  - PASS: Traces correlate tenant and identity.

  - FAIL: Traces lack tenant information.


  **Are audit events immutable and tenant-scoped?**


  - PASS: Audit logs are append-only and scoped.

  - FAIL: Audit records can be modified.


  **Are lifecycle, identity, and policy changes auditable?**


  - PASS: All changes emit audit events.

  - FAIL: Changes occur silently.


  **Are Safety Gates evaluated with full Tenant Context?**


  - PASS: Gates receive tenant and identity.

  - FAIL: Gates evaluate without tenant scope.


  **Do Safety Gates emit tenant-scoped telemetry?**


  - PASS: Gate outcomes are logged per tenant.

  - FAIL: Gate outcomes are global.


  **Is blast radius attributable to a single tenant?**


  - PASS: Incidents are traceable to one tenant.

  - FAIL: Impact cannot be localized.


  **Can execution paths be reconstructed end-to-end per tenant?**


  - PASS: Full trace reconstruction is possible.

  - FAIL: Gaps prevent reconstruction.


  ---'
source_documents:
- path: architecture_library/31_contura_multi_tenancy_validation_user_guide_v1.md
  section_heading: 7. Observability, Audit & Safety Validation
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '## 7.'
  - Observability, Audit & Safety Validation **Do all logs include tenant identifiers?** - PASS
  definition_of_done:
  - 'Verify that ## 7.'
  - Verify that observability, Audit & Safety Validation **Do all logs include tenant identifiers?** - PASS
  capabilities_needed:
  - tenancy_context
  - identity_access
  - tenant_lifecycle
  - tenancy_isolation
  - observability_audit
  - policy_enforcement
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
