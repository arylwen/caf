pattern_id: CAF-MTEN-LC-04
name: Tenant Deletion and Retention
summary: Tenant deletion is irreversible and high-risk.
intent: Tenant deletion is irreversible and high-risk.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: '#### 5.5.7 Tenant Deletion and Retention


  Tenant deletion is irreversible and high-risk.


  Normative rules:


  - Deletion MUST be explicit and deliberate

  - Active execution MUST be halted before deletion

  - Data retention policies MUST be enforced consistently


  Deletion patterns MUST:


  - Avoid orphaned resources

  - Respect compliance and retention obligations

  - Be auditable end-to-end


  ---'
source_documents:
- path: architecture_library/21_contura_multi_tenancy_patterns_v1.md
  section_heading: 5.5.7 Tenant Deletion and Retention
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '- Deletion MUST be explicit and deliberate - Active execution MUST be halted before deletion - Data retention policies
    MUST be enforced consistently Deletion patterns MUST'
  definition_of_done:
  - Verify that - Deletion MUST be explicit and deliberate - Active execution MUST be halted before deletion - Data retention
    policies MUST be enforced consistently Deletion patterns MUST
  capabilities_needed:
  - tenancy_context
  - audit_retention
  - tenant_deletion_retention
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
