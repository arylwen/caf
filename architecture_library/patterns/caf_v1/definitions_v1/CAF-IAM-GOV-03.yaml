pattern_id: CAF-IAM-GOV-03
name: Identity Integration Governance
summary: The Control Plane governs how external identity providers integrate with the platform.
intent: The Control Plane governs how external identity providers integrate with the platform.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
  refines: CAF-IAM-GOV-01
extended_definition: '### Identity Integration Governance Pattern


  The Control Plane governs how external identity providers integrate with the platform.


  Under this pattern:


  - Supported identity integration models are defined centrally

  - Trust boundaries with external providers are explicit

  - Token semantics, claims usage, and mapping rules are governed

  - Changes to identity integration require controlled evolution


  Application Plane systems rely on these integrations but do not define them.


  ---'
source_documents:
- path: architecture_library/22_contura_identity_access_pattern_guide_v1.md
  section_heading: Identity Integration Governance Pattern
caf:
  layer: caf
  verifiability: plan_ready
  plane: control
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '- Supported identity integration models are defined centrally - Trust boundaries with external providers are explicit
    - Token semantics, claims usage, and mapping rules are governed - Changes to identity integration require controlled evolution
    Application Plane systems rely on these integrations but do not define them.'
  definition_of_done:
  - 'Verify that ### Identity Integration Governance Pattern The Control Plane governs how external identity providers integrate
    with the platform.'
  - Verify that under this pattern
  capabilities_needed:
  - identity_authentication
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: iam.identity_integration_model
    description: How external identity providers integrate (single vs federated).
    options:
    - option_id: single_idp
      summary: Single primary IdP for all tenants.
      integration_model: single_idp
      notes: ''
    - option_id: federated_sso_per_tenant
      summary: Tenants bring their own IdP/SSO; federation per tenant.
      integration_model: federated_sso_per_tenant
      notes: ''
    - option_id: hybrid
      summary: Default IdP plus optional per-tenant federation.
      integration_model: hybrid
      notes: ''
    - option_id: custom
      summary: Custom (fill fields).
      integration_model: ''
      notes: ''
    default_option_id: hybrid
  human_questions:
  - question_id: Q-IAM-IDENTITY_INTEGRATION_MODEL-01
    question: Select an option for iam.identity_integration_model and fill any required fields.
    option_set_id: iam.identity_integration_model
    status: open
    answer: {}
