pattern_id: CAF-IAM-AUTH-05
name: Agent and Service Authorization
summary: Application Plane systems enforce authorization for services and agents using the same governance boundaries as for
  users.
intent: Application Plane systems enforce authorization for services and agents using the same governance boundaries as for
  users.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
  refines: CAF-IAM-AUTH-01
extended_definition: '### Agent and Service Authorization Pattern


  Application Plane systems enforce authorization for services and agents using the same governance boundaries as for users.


  Under this pattern:


  - Agents and services operate under explicit, governed identities

  - Authorization checks apply uniformly to human and non-human principals

  - Agent actions are constrained by policy, safety gates, and identity scope

  - No implicit elevation is granted to automation or AI behavior


  Automation does not bypass authorization.


  ---'
source_documents:
- path: architecture_library/22_contura_identity_access_pattern_guide_v1.md
  section_heading: Agent and Service Authorization Pattern
caf:
  layer: caf
  verifiability: plan_ready
  plane: application
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '### Agent and Service Authorization Pattern Application Plane systems enforce authorization for services and agents using
    the same governance boundaries as for users.'
  - Under this pattern
  definition_of_done:
  - Verify that - Agents and services operate under explicit, governed identities - Authorization checks apply uniformly to
    human and non-human principals - Agent actions are constrained by policy, safety gates, and identity scope - No implicit
    elevation is granted to automation or AI behavior Automation does not bypass authorization.
  capabilities_needed:
  - identity_authentication
  - identity_authorization
  - policy_enforcement
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: iam.nonhuman_authorization_strategy
    description: How services and agents are authorized (policy scope and mechanism).
    options:
    - option_id: same_policy_engine
      summary: Use the same policy engine and namespace as user authZ
      policy_scope: shared
      mechanism: policy_engine
    - option_id: separate_namespace
      summary: Same engine, separate namespace for non-human principals
      policy_scope: separate_namespace
      mechanism: policy_engine
    - option_id: capability_tokens
      summary: Authorize via scoped capability tokens issued by Control Plane
      policy_scope: token_scopes
      mechanism: capability_token
    - option_id: static_role_binding
      summary: Static role bindings for service accounts (least flexible)
      policy_scope: static_roles
      mechanism: roles
    - option_id: custom
      summary: Custom (fill details; prefer adopting a retrieved option when available)
    default_option_id: same_policy_engine
  human_questions:
  - question_key: iam.nonhuman_authorization_strategy
    question_id: Q-IAM-NONHUMAN-AUTHZ-01
    question: How should services and agents be authorized?
    description: Choose the authorization strategy for non-human principals and note any required scopes.
    option_set_id: iam.nonhuman_authorization_strategy
    specialization_notes:
    - Prefer policy-engine-based governance unless capability tokens are required for offline/async.
    anchors:
    - caf_pattern: CAF-IAM-AUTH-05
