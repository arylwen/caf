pattern_id: CAF-DG-07
name: Data Lifecycle Governance (Creation to Deletion)
summary: All data MUST have an explicit lifecycle.
intent: All data MUST have an explicit lifecycle.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
extended_definition: '## 7. Data Lifecycle Governance (Creation to Deletion)


  All data MUST have an explicit lifecycle.


  Lifecycle stages include:


  - Creation

  - Active use

  - Transformation

  - Archival

  - Deletion


  Lifecycle transitions MUST:


  - Be explicitly triggered

  - Be policy-governed

  - Be auditable

  - Preserve lineage


  Deletion MUST be:


  - Deterministic

  - Irreversible at the logical level

  - Provable via audit evidence


  Data retention without explicit intent is prohibited.


  ---'
source_documents:
- path: architecture_library/23_contura_data_governance_and_data_quality_standards_v1.md
  section_heading: 7. Data Lifecycle Governance (Creation to Deletion)
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - Data Lifecycle Governance (Creation to Deletion) All data MUST have an explicit lifecycle.
  - '- Creation - Active use - Transformation - Archival - Deletion Lifecycle transitions MUST'
  - '- Be explicitly triggered - Be policy-governed - Be auditable - Preserve lineage Deletion MUST be'
  definition_of_done:
  - Verify that data Lifecycle Governance (Creation to Deletion) All data MUST have an explicit lifecycle.
  - Verify that - Creation - Active use - Transformation - Archival - Deletion Lifecycle transitions MUST
  - Verify that - Be explicitly triggered - Be policy-governed - Be auditable - Preserve lineage Deletion MUST be
  - Verify that - Deterministic - Irreversible at the logical level - Provable via audit evidence Data retention without explicit
    intent is prohibited.
  capabilities_needed:
  - data_lifecycle
  - data_observability_lineage
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: data_lifecycle.retention_mode
    description: How retention and deletion are governed and enforced.
    options:
    - option_id: policy_by_class
      summary: Retention by classification/policy class (central policy defines schedules).
      mode: policy_by_class
    - option_id: record_ttl
      summary: Record-level TTL/expiry field enforced at runtime.
      mode: record_ttl
    - option_id: administrative_only
      summary: Administrative/manual retention and deletion operations only.
      mode: administrative_only
    - option_id: custom
      summary: Custom (fill fields).
      mode: ''
    default_option_id: policy_by_class
  human_questions:
  - question_id: Q-CAF-DG-07-01
    topic: data_lifecycle.retention_mode
    question: Which retention mode will you adopt, and what are the key constraints (legal/compliance/ops)?
    status: open
    option_set_id: data_lifecycle.retention_mode
    answer: {}
