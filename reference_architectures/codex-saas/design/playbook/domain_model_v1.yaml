schema_version: domain_model_v1
generated_from:
  inputs:
    - reference_architectures/codex-saas/spec/playbook/application_spec_v1.md
    - reference_architectures/codex-saas/spec/playbook/system_spec_v1.md
domain:
  summary: >-
    The product manages tenant-scoped widgets with structured text content and
    supports standard CRUD-style interactions over those records.
  bounded_contexts:
    - context_id: CORE
      name: Core
      description: Core context for managing widget records and their lifecycle operations.
      aggregates:
        - name: WidgetCatalog
          entities:
            - name: Widget
              fields:
                - name: name
                  type: text
                  required: true
                - name: description
                  type: text
                  required: true
                - name: content
                  type: text
                  required: true
  use_cases:
    - use_case_id: UC-01
      title: Manage Widgets
      intent: Create, read, update, and delete widget records.
      primary_context_id: CORE
      touches_entities:
        - Widget
    - use_case_id: UC-02
      title: List Widgets
      intent: List available widget records for user review.
      primary_context_id: CORE
      touches_entities:
        - Widget
api_candidates:
  resources:
    - name: Widget
      operations:
        - list
        - get
        - create
        - update
        - delete
