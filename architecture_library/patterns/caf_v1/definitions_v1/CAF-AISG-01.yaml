pattern_id: CAF-AISG-01
name: Definition of an AI Safety Gate
summary: An AI Safety Gate is a structured evaluation mechanism that determines whether an AI component or workflow is allowed
  to execute, escalate authority, perform a high-impact action, or graduate between lifecycle stages....
intent: An AI Safety Gate is a structured evaluation mechanism that determines whether an AI component or workflow is allowed
  to execute, escalate authority, perform a high-impact action, or graduate between lifecycle stages....
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
extended_definition: '## Definition of an AI Safety Gate


  An AI Safety Gate is a structured evaluation mechanism that determines whether an AI component or workflow is allowed to
  execute, escalate authority, perform a high-impact action, or graduate between lifecycle stages. Safety Gates apply to:


  - Agents (planning, orchestration, autonomous loops)

  - Models (LLMs, embeddings, fine-tuned models)

  - Tools (agent-accessible actions, APIs, platform capabilities)

  - Reasoning traces (intermediate thoughts, chains of reasoning)

  - Workflows (retrieval-augmented, policy-governed, multi-agent)

  - User-facing generative interactions

  - System-level AI behaviors


  A Safety Gate may be invoked:


  - **Pre-execution** (prevent unsafe execution)

  - **Mid-execution** (halt or redirect during anomalies)

  - **Post-execution** (validate outputs before externalization)'
source_documents:
- path: architecture_library/10_contura_ai_safety_gate_specification_v1.md
  section_heading: Definition of an AI Safety Gate
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '## Definition of an AI Safety Gate An AI Safety Gate is a structured evaluation mechanism that determines whether an
    AI component or workflow is allowed to execute, escalate authority, perform a high-impact action, or graduate between
    lifecycle stages.'
  - Safety Gates apply to
  definition_of_done:
  - 'Verify that ## Definition of an AI Safety Gate An AI Safety Gate is a structured evaluation mechanism that determines
    whether an AI component or workflow is allowed to execute, escalate authority, perform a high-impact action, or graduate
    between lifecycle stages.'
  - Verify that safety Gates apply to
  capabilities_needed:
  - safety_gating
  - observability_emit
  - policy_enforcement
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  option_sets:
  - option_set_id: safety_gate.invocation_stage
    summary: Select the primary stage where Safety Gates are invoked for the critical path.
    options:
    - option_id: pre_execution
      status: defer
      summary: Pre-execution gating (preferred for high-impact actions).
    - option_id: mid_execution
      status: defer
      summary: Mid-execution gating/monitoring (for long-running workflows).
    - option_id: post_execution
      status: defer
      summary: Post-execution validation (for audits, promotions, or deferred checks).
    - option_id: custom
      status: defer
      summary: Custom (describe gating stages and triggers).
      notes: ''
    default_option_id: pre_execution
  human_questions:
  - question_key: safety_gate_invocation_stage
    question_id: Q-AISG-STAGE-01
    question: At what stage should Safety Gates be invoked for the primary workflow?
    description: Select one option; use 'custom' only if none fit.
    option_set_id: safety_gate.invocation_stage
    anchors:
    - caf_pattern: CAF-AISG-01
  kind: decision_pattern
