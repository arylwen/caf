pattern_id: CAF-MTEN-ANTI-03
name: Coupling Billing, Authorization, and Isolation
summary: Derived from architecture_library/21_contura_multi_tenancy_patterns_v1.md section '7.3 Coupling Billing, Authorization,
  and Isolation'.
intent: Derived from 7.3 Coupling Billing, Authorization, and Isolation.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: |
  ### 7.3 Coupling Billing, Authorization, and Isolation

  #### **Coupling Billing, Authorization, and Isolation Description**  

  Cost centers, plans, or billing tiers are used as:

  - Authorization mechanisms
  - Isolation selectors
  - Routing logic

  Examples:

  - “Enterprise plan gets dedicated DB”
  - “Free plan users can’\
    t access this endpoint”
  - “Cost center determines data visibility”

  #### **Coupling Billing, Authorization, and Isolation: Why It Happens**

  - Pressure to monetize quickly
  - Overloading billing metadata
  - Shortcutting governance design

  #### **Coupling Billing, Authorization, and Isolation: Why It Fails**

  - Security vulnerabilities
  - Inconsistent enforcement
  - Entitlement drift
  - Compliance failures

  #### **Coupling Billing, Authorization, and Isolation: Normative Rule**

  > Billing signals MAY inform Cost Governance; billing MUST NOT be treated as governance.

  Cost is metadata.
  \
    Isolation and authorization are security primitives.

  ---
source_documents:
- path: architecture_library/21_contura_multi_tenancy_patterns_v1.md
  section_heading: 7.3 Coupling Billing, Authorization, and Isolation
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '### 7.3 Coupling Billing, Authorization, and Isolation #### **Coupling Billing, Authorization, and Isolation Description**
    Cost centers, plans, or billing tiers are used as'
  - billing MUST NOT be treated as governance.
  definition_of_done:
  - Verify that billing MUST NOT be treated as governance.
  capabilities_needed:
  - metering_usage
  - identity_access
  - tenancy_isolation
  - enterprise_compliance
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
