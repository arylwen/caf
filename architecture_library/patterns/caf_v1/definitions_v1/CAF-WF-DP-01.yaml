pattern_id: CAF-WF-DP-01
name: 'Data Plane: No Orchestration of Governance'
summary: '- Data Plane may orchestrate **data-internal** tasks (e.g., index maintenance), but **not** tenant lifecycle or
  platform policies.'
intent: '- Data Plane may orchestrate **data-internal** tasks (e.g., index maintenance), but **not** tenant lifecycle or platform
  policies.'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: OBS-01
  complements: CTX-01
  complements: POL-01
extended_definition: |
  ### Data Plane: No Orchestration of Governance

  - Data Plane may orchestrate **data-internal** tasks (e.g., index maintenance), but **not** tenant lifecycle or platform policies.

  **Normative statement:**  

  > All platform-level workflows (tenant lifecycle, policy rollout, AI configuration) **MUST** be orchestrated by the Control Plane.  
  > Tenant business workflows **MUST** be orchestrated in the Application Plane.

  ---
source_documents:
- path: architecture_library/20_contura_control_application_data_plane_pattern_guide_v1.md
  section_heading: 'Data Plane: No Orchestration of Governance'
caf:
  layer: caf
  verifiability: plan_ready
  plane: data
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '**Normative statement:** > All platform-level workflows (tenant lifecycle, policy rollout, AI configuration) **MUST**
    be orchestrated by the Control Plane.'
  - '> Tenant business workflows **MUST** be orchestrated in the Application Plane.'
  definition_of_done:
  - Verify that **Normative statement:** > All platform-level workflows (tenant lifecycle, policy rollout, AI configuration)
    **MUST** be orchestrated by the Control Plane.
  - Verify that > Tenant business workflows **MUST** be orchestrated in the Application Plane.
  capabilities_needed:
  - workflow_orchestration
  - policy_enforcement
  - ai_safety_integration
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
