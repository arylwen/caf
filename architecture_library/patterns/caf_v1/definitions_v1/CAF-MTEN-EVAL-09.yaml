pattern_id: CAF-MTEN-EVAL-09
name: Routing & Context Propagation
summary: Tenant context resolution at ingress is defined. Tenant context propagation rules are explicit. It is clear how tenant context flows through services. Routing decisions are consistent with Control Plane metadata. Misr...
intent: Tenant context resolution at ingress is defined.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: '## 9. Routing & Context Propagation


  - [ ] Tenant context resolution at ingress is defined.

  - [ ] Tenant context propagation rules are explicit.

  - [ ] It is clear how tenant context flows through services.

  - [ ] Routing decisions are consistent with Control Plane metadata.

  - [ ] Misrouting risks are acknowledged.


  ---'
source_documents:
- path: architecture_library/90_multi_tenancy_patterns_evaluation_and_completeness_checklist_v1.md
  section_heading: 9. Routing & Context Propagation
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '## 9.'
  - Routing & Context Propagation - [ ] Tenant context resolution at ingress is defined.
  definition_of_done:
  - 'Verify that ## 9.'
  - Verify that routing & Context Propagation - [ ] Tenant context resolution at ingress is defined.
  capabilities_needed:
  - plane_integrity
  - context_propagation
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: checklist
