pattern_id: CAF-IAM-OBS-02
name: End-to-End Traceability
summary: Identity context must be traceable end-to-end across planes and workflows.
intent: Identity context must be traceable end-to-end across planes and workflows.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
  refines: CAF-IAM-OBS-01
extended_definition: '### End-to-End Traceability Pattern


  Identity context must be traceable end-to-end across planes and workflows.


  Under this pattern:


  - Identity and tenant context are preserved across service boundaries

  - Context is maintained across synchronous, asynchronous, and deferred execution

  - Traces link upstream intent to downstream execution

  - Background or retried work retains originating identity context


  Traceability ensures that distributed systems behave as a coherent whole.


  ---'
source_documents:
- path: architecture_library/22_contura_identity_access_pattern_guide_v1.md
  section_heading: End-to-End Traceability Pattern
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '### End-to-End Traceability Pattern Identity context must be traceable end-to-end across planes and workflows.'
  definition_of_done:
  - 'Verify that ### End-to-End Traceability Pattern Identity context must be traceable end-to-end across planes and workflows.'
  capabilities_needed:
  - identity_authentication
  - tenancy_context
  - observability_emit
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: iam.trace_correlation_mode
    description: How identity context is correlated end-to-end across planes, workflows, and async boundaries.
    options:
    - option_id: trace_id_propagation
      summary: Propagate a trace_id/correlation_id through all hops
      correlation_field: trace_id
    - option_id: span_context
      summary: Distributed tracing span context (trace/span) across services
      correlation_field: trace/span
    - option_id: event_correlation
      summary: Event IDs and causal links for async workflows
      correlation_field: event_id/causal_links
    - option_id: hybrid
      summary: Use span context for sync; event correlation for async
      correlation_field: hybrid
    - option_id: custom
      summary: Custom (fill details; prefer adopting a retrieved option when available)
    default_option_id: hybrid
  human_questions:
  - question_key: auto_iam_trace_correlation_mode
    question_id: Q-CAF-IAM-OBS-02-AUTO-01
    question: How identity context is correlated end-to-end across planes, workflows, and async boundaries.
    description: How identity context is correlated end-to-end across planes, workflows, and async boundaries.
    option_set_id: iam.trace_correlation_mode
    specialization_notes:
    - 'Auto-generated: this pattern declares option_sets but lacked caf.human_questions.'
    - Refine or replace this question text and specialization notes in the library if needed.
    anchors:
    - caf_pattern: CAF-IAM-OBS-02
