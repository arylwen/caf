pattern_id: CAF-DG-02
name: Why Data Governance Must Be Architectural
summary: In Contura systems, data governance is not a policy overlay or organizational process.
intent: In Contura systems, data governance is not a policy overlay or organizational process.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
extended_definition: '## 2. Why Data Governance Must Be Architectural


  In Contura systems, data governance is not a policy overlay or organizational process.


  It is an **architectural constraint**.


  AI-first, multi-tenant SaaS platforms introduce failure modes where:


  - Data is reused outside its intended scope

  - Data quality degrades silently over time

  - Lineage is lost across agentic workflows

  - Trust assumptions become implicit

  - Violations are detected only after harm occurs


  Therefore:


  > **Data governance MUST be designed into the system architecture, not layered on afterward.**


  Architectural data governance ensures that:


  - Violations fail closed

  - Data usage is explainable

  - Quality is continuously validated

  - Trust is computable, not assumed


  ---'
source_documents:
- path: architecture_library/23_contura_data_governance_and_data_quality_standards_v1.md
  section_heading: 2. Why Data Governance Must Be Architectural
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - Why Data Governance Must Be Architectural In Contura systems, data governance is not a policy overlay or organizational
    process.
  - '> **Data governance MUST be designed into the system architecture, not layered on afterward.** Architectural data governance
    ensures that'
  definition_of_done:
  - Verify that why Data Governance Must Be Architectural In Contura systems, data governance is not a policy overlay or organizational
    process.
  - Verify that > **Data governance MUST be designed into the system architecture, not layered on afterward.** Architectural
    data governance ensures that
  capabilities_needed:
  - data_governance
  - data_quality
  - data_observability_lineage
  - ai_data_constraints
  - anti_pattern_detection
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
