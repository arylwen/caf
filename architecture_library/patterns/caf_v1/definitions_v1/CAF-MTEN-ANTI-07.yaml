pattern_id: CAF-MTEN-ANTI-07
name: Agent Autonomy Without Boundaries
summary: Derived from architecture_library/21_contura_multi_tenancy_patterns_v1.md section '7.7 Agent Autonomy Without Boundaries'.
intent: Derived from 7.7 Agent Autonomy Without Boundaries.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: |
  ### 7.7 Agent Autonomy Without Boundaries

  #### **Agent Autonomy Without Boundaries: Description**
   nAgents are introduced without:

  - Explicit tenant identity
  - Cost budgets
  - Lifecycle binding
  - Observability

  \
    Examples:

  - Agents executing indefinitely
  - Agents sharing memory across tenants
  - Agents invoking tools without tenant checks

  #### **Agent Autonomy Without Boundaries: Why It Happens**

  - Underestimating AI blast radius
  - Treating agents as “helpers” instead of actors

  #### **Agent Autonomy Without Boundaries: Why It Fails**

  - Unbounded cost
  - Cross-tenant leakage
  - Safety incidents
  - Irreversible trust loss

  #### **Agent Autonomy Without Boundaries: Normative Rule**

  > An agent is a first-class principal, not a background task.

  Agents without explicit boundaries are architectural defects.

  ---
source_documents:
- path: architecture_library/21_contura_multi_tenancy_patterns_v1.md
  section_heading: 7.7 Agent Autonomy Without Boundaries
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '### 7.7 Agent Autonomy Without Boundaries #### **Agent Autonomy Without Boundaries'
  - Description** Agents are introduced without
  definition_of_done:
  - 'Verify that ### 7.7 Agent Autonomy Without Boundaries #### **Agent Autonomy Without Boundaries'
  - Verify that description** Agents are introduced without
  capabilities_needed:
  - tenancy_context
  - observability_emit
  - identity_access
  - tenant_lifecycle
  - agent_boundary
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
