pattern_id: CAF-IAM-PROP-03
name: Policy Signals vs Control Signals
summary: Policy evaluation outcomes are not control intent.
intent: Policy evaluation outcomes are not control intent.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
  refines: CAF-IAM-PROP-01
extended_definition: '### Policy Signals vs Control Signals Pattern


  Policy evaluation outcomes are not control intent.


  Under this pattern:


  - Policy allow/deny outcomes affect runtime behavior only

  - Authorization failures do not trigger lifecycle or governance changes

  - Policy signals are not interpreted as control directives

  - Control intent remains explicit and centrally governed


  Policy constrains behavior; control directs the system.


  ---


  **Normative statement:**


  > Policy intent is centrally governed, explicitly delegated, and locally enforced; no plane may assume, reinterpret, or
  propagate policy authority beyond its delegated boundaries.'
source_documents:
- path: architecture_library/22_contura_identity_access_pattern_guide_v1.md
  section_heading: Policy Signals vs Control Signals Pattern
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '### Policy Signals vs Control Signals Pattern Policy evaluation outcomes are not control intent.'
  - Under this pattern
  definition_of_done:
  - Verify that no plane may assume, reinterpret, or propagate policy authority beyond its delegated boundaries.
  capabilities_needed:
  - identity_authorization
  - policy_enforcement
  - delegation_boundary
  - failure_handling
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: signals.separation_mode
    summary: Choose among bounded variants for `signals.separation_mode` (or use `custom`).
    options:
    - option_id: separate_channels
      status: defer
      summary: Separate channels for policy signals vs control signals (distinct endpoints/topics).
    - option_id: tagged_unified_channel
      status: defer
      summary: Unified channel with explicit message typing and strict handlers per type.
    - option_id: control_plane_only_for_control
      status: defer
      summary: Control signals only via Control Plane APIs; policy signals may be broader.
    - option_id: custom
      status: defer
      summary: Custom (describe your approach; prefer adopting a bounded variant when possible).
      notes: ''
    default_option_id: separate_channels
  human_questions:
  - question_key: auto_signals_separation_mode
    question_id: Q-CAF-IAM-PROP-03-AUTO-01
    question: Select an option for signals.separation_mode.
    description: ''
    option_set_id: signals.separation_mode
    specialization_notes:
    - 'Auto-generated: this pattern declares option_sets but lacked caf.human_questions.'
    - Refine or replace this question text and specialization notes in the library if needed.
    anchors:
    - caf_pattern: CAF-IAM-PROP-03
