pattern_id: CAF-SAFE-DP-01
name: Data Plane Responsibilities (Safety & Guardrails)
summary: '- Store Safety Gate logs where needed. - Ensure that AI-related data (e.g., prompts, outputs) are retained or deleted
  per policy.'
intent: '- Store Safety Gate logs where needed.'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: OBS-01
  complements: CAF-POL-01
  complements: CAF-TCTX-01
extended_definition: |
  ### Data Plane Responsibilities (Safety & Guardrails)

  - Store Safety Gate logs where needed.  
  \
    - Ensure that AI-related data (e.g., prompts, outputs) are retained or deleted per policy.  

  Any AI workflow that bypasses configured Safety Gates is **non-compliant**.

  ---
source_documents:
- path: architecture_library/20_contura_control_application_data_plane_pattern_guide_v1.md
  section_heading: Data Plane Responsibilities (Safety & Guardrails)
caf:
  layer: caf
  verifiability: plan_ready
  plane: data
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '### Data Plane Responsibilities (Safety & Guardrails) - Store Safety Gate logs where needed.'
  definition_of_done:
  - Verify that - Ensure that AI-related data (e.g., prompts, outputs) are retained or deleted per policy.
  capabilities_needed:
  - workflow_orchestration
  - safety_gating
  - policy_enforcement
  - ai_safety_integration
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
  option_sets:
  - option_set_id: dp_safety.enforcement_level
    description: Baseline enforcement level for Data Plane safety obligations (guardrails, checks, and fail-closed behavior).
    options:
    - option_id: strict_fail_closed
      summary: Fail-closed on policy/tenant boundary violations; enforce mandatory checks at boundaries.
    - option_id: standard
      summary: Standard enforcement with explicit checks at key boundaries; log and block critical violations.
    - option_id: custom
      summary: Custom enforcement level (document details).
    default_option_id: standard
  human_questions:
  - question_key: dp_safety_enforcement_level
    question_id: Q-SAFE-DP-LEVEL-01
    question: What baseline enforcement level should Data Plane safety obligations use?
    description: Select the default enforcement posture for safety checks at Data Plane boundaries. Default should be adopted
      unless conflicts exist.
    option_set_id: dp_safety.enforcement_level
    anchors:
    - caf_pattern: CAF-SAFE-DP-01
