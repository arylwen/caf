pattern_id: CAF-MTEN-VALU-06
name: Cost & Usage Enforcement Validation
summary: '**Is usage metered at well-defined execution boundaries?**'
intent: '**Is usage metered at well-defined execution boundaries?**'
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: POL-01
  refines: CAF-MTEN-01
  complements: PST-01
extended_definition: '## 6. Cost & Usage Enforcement Validation


  **Is usage metered at well-defined execution boundaries?**


  - PASS: Each API call emits a metering event.

  - FAIL: Usage is estimated offline.


  **Does every metering event include tenant identifier and acting principal?**


  - PASS: Metering records include tenant and identity.

  - FAIL: Metering aggregates across tenants.


  **Is AccountScope the authoritative source of entitlements and quotas?**


  - PASS: Runtime checks query AccountScope policy.

  - FAIL: Services define local limits.


  **Are entitlements enforced before or during execution?**


  - PASS: Requests are rejected when limits are exceeded.

  - FAIL: Limits are enforced only after billing.


  **Are long-running executions subject to continuous budget checks?**


  - PASS: Budgets are checked periodically.

  - FAIL: Long jobs run unchecked.


  **Are agents assigned explicit, tenant-scoped cost budgets?**


  - PASS: Agents stop when budgets are exhausted.

  - FAIL: Agents run indefinitely.


  **Can agent or workflow execution be interrupted on budget exhaustion?**


  - PASS: Execution halts safely.

  - FAIL: Execution continues despite exhaustion.


  **Are CostCenters limited to attribution only?**


  - PASS: CostCenters do not affect execution.

  - FAIL: CostCenters gate access.


  **Is anomalous tenant spend detectable in near real time?**


  - PASS: Alerts trigger on spend spikes.

  - FAIL: Spend anomalies are discovered post-factum.


  ---'
source_documents:
- path: architecture_library/31_contura_multi_tenancy_validation_user_guide_v1.md
  section_heading: 6. Cost & Usage Enforcement Validation
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - Requests are rejected when limits are exceeded.
  - Agents stop when budgets are exhausted.
  definition_of_done:
  - Verify that **Does every metering event include tenant identifier and acting principal?** - PASS
  capabilities_needed:
  - tenancy_context
  - identity_access
  - metering_usage
  - agent_boundary
  - policy_enforcement
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: pattern
