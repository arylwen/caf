pattern_id: CAF-POL-S13
name: Policy Observability, Evidence, and Drift
summary: Every policy decision MUST produce evidence.
intent: Every policy decision MUST produce evidence.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
extended_definition: '## 13. Policy Observability, Evidence, and Drift


  Every policy decision MUST produce evidence.


  Policy observability MUST include:


  - Policy identifier and version

  - Evaluation inputs (hashed or referenced)

  - Evaluation outcome

  - Enforcement action

  - Tenant and identity scope

  - Timestamp and plane


  Policy drift MUST be detectable via:


  - Outcome trend analysis

  - Violation frequency

  - Behavioral deviation


  Undetected drift is a governance failure.


  ---'
source_documents:
- path: architecture_library/22_contura_policy_engine_architecture_guide_v1.md
  section_heading: 13. Policy Observability, Evidence, and Drift
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - Policy Observability, Evidence, and Drift Every policy decision MUST produce evidence.
  - Policy observability MUST include
  - '- Policy identifier and version - Evaluation inputs (hashed or referenced) - Evaluation outcome - Enforcement action
    - Tenant and identity scope - Timestamp and plane Policy drift MUST be detectable via'
  definition_of_done:
  - Verify that policy Observability, Evidence, and Drift Every policy decision MUST produce evidence.
  - Verify that policy observability MUST include
  - Verify that - Policy identifier and version - Evaluation inputs (hashed or referenced) - Evaluation outcome - Enforcement
    action - Tenant and identity scope - Timestamp and plane Policy drift MUST be detectable via
  capabilities_needed:
  - observability_emit
  - compliance_evidence
  - policy_enforcement
  - drift_detection
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: policy.evidence_capture_level
    description: How much evidence is recorded for each policy evaluation and enforcement action.
    options:
    - option_id: decision_only
      summary: Record decision outcome + minimal metadata
      evidence_level: minimal
    - option_id: decision_plus_inputs_hash
      summary: Record decision + hashed inputs for reproducibility
      evidence_level: inputs_hash
    - option_id: explain_trace
      summary: Record reason codes/explain trace with key inputs
      evidence_level: explain
    - option_id: sampled_full_trace
      summary: Full trace for sampled subset; minimal otherwise
      evidence_level: sampled_full
    - option_id: custom
      summary: Custom (fill details; prefer adopting a retrieved option when available)
    default_option_id: decision_plus_inputs_hash
  human_questions:
  - question_key: policy.evidence_capture_level
    question_id: Q-POL-EVID-01
    question: What evidence capture level is required for policy decisions?
    description: Choose the minimum evidence that must be produced for audit/debugging/drift detection.
    option_set_id: policy.evidence_capture_level
    specialization_notes:
    - If explainability is required, prefer reason codes with stable identifiers.
    anchors:
    - caf_pattern: CAF-POL-S13
