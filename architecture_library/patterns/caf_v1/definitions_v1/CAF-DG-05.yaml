pattern_id: CAF-DG-05
name: Data Classification and Sensitivity Domains
summary: All data assets MUST be classified.
intent: All data assets MUST be classified.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
extended_definition: '## 5. Data Classification and Sensitivity Domains


  All data assets MUST be classified.


  Classification MUST be:


  - Explicit

  - Machine-readable

  - Enforced at runtime

  - Versioned


  At minimum, classification MUST capture:


  - Tenant scope

  - Sensitivity level

  - Usage constraints

  - Retention requirements


  Classification MAY evolve, but changes MUST be governed via ADRs and policy updates.


  Data without classification MUST be treated as **non-compliant**.


  ---'
source_documents:
- path: architecture_library/23_contura_data_governance_and_data_quality_standards_v1.md
  section_heading: 5. Data Classification and Sensitivity Domains
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - Data Classification and Sensitivity Domains All data assets MUST be classified.
  - Classification MUST be
  - '- Explicit - Machine-readable - Enforced at runtime - Versioned At minimum, classification MUST capture'
  definition_of_done:
  - Verify that data Classification and Sensitivity Domains All data assets MUST be classified.
  - Verify that classification MUST be
  - Verify that - Explicit - Machine-readable - Enforced at runtime - Versioned At minimum, classification MUST capture
  - Verify that - Tenant scope - Sensitivity level - Usage constraints - Retention requirements Classification MAY evolve,
    but changes MUST be governed via ADRs and policy updates.
  - Verify that data without classification MUST be treated as **non-compliant**.
  capabilities_needed:
  - data_classification
  - data_lifecycle
  - ai_data_constraints
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: data_classification.scheme
    description: Choose a bounded, machine-readable data classification scheme.
    options:
    - option_id: four_tier
      summary: Four-tier scheme (public/internal/confidential/restricted).
      levels:
      - public
      - internal
      - confidential
      - restricted
      default_level: internal
    - option_id: three_tier
      summary: Three-tier scheme (public/internal/confidential).
      levels:
      - public
      - internal
      - confidential
      default_level: internal
    - option_id: two_tier
      summary: Two-tier scheme (public/sensitive).
      levels:
      - public
      - sensitive
      default_level: public
    - option_id: custom
      summary: Custom (fill fields).
      levels: []
      default_level: ''
    default_option_id: four_tier
  human_questions:
  - question_id: Q-CAF-DG-05-01
    topic: data_classification.scheme
    question: Which data classification scheme will you adopt for this system?
    status: open
    option_set_id: data_classification.scheme
    answer: {}
