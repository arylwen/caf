pattern_id: CAF-COST-01
name: Cost as a First-Class Resource
summary: Cost MUST be treated as a **first-class governed resource**.
intent: Cost MUST be treated as a **first-class governed resource**.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
extended_definition: |
  ## 3. Cost as a First-Class Resource

  Cost MUST be treated as a **first-class governed resource**.
   nThis implies:

  - Cost has **identity** (who incurred it)  
  - Cost has **scope** (which tenant, agent, workflow, plane)  
  - Cost has **policy** (budgets, quotas, entitlements)  
  - Cost has **enforcement points** (pre, mid, post execution)  
  - Cost has **observability and audit trails**  

  Cost MUST NOT be:

  - Implicit  
  - Post-hoc only  
  - Aggregated without attribution  
  - Governed solely by external billing systems  

  Every unit of cost MUST be attributable and governable.

  ---
source_documents:
- path: architecture_library/12_contura_cost_governance_finops_v1.md
  section_heading: 3. Cost as a First-Class Resource
caf:
  layer: caf
  verifiability: plan_ready
  plane: both
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - Cost as a First-Class Resource Cost MUST be treated as a **first-class governed resource**.
  - '- Cost has **identity** (who incurred it) - Cost has **scope** (which tenant, agent, workflow, plane) - Cost has **policy**
    (budgets, quotas, entitlements) - Cost has **enforcement points** (pre, mid, post execution) - Cost has **observability
    and audit trails** Cost MUST NOT be'
  - '- Implicit - Post-hoc only - Aggregated without attribution - Governed solely by external billing systems Every unit
    of cost MUST be attributable and governable.'
  definition_of_done:
  - Verify that cost as a First-Class Resource Cost MUST be treated as a **first-class governed resource**.
  - Verify that - Cost has **identity** (who incurred it) - Cost has **scope** (which tenant, agent, workflow, plane) - Cost
    has **policy** (budgets, quotas, entitlements) - Cost has **enforcement points** (pre, mid, post execution) - Cost has
    **observability and audit trails** Cost MUST NOT be
  - Verify that - Implicit - Post-hoc only - Aggregated without attribution - Governed solely by external billing systems
    Every unit of cost MUST be attributable and governable.
  capabilities_needed:
  - safety_gating
  - observability_emit
  - cost_governance
  - compliance_evidence
  - policy_enforcement
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: cost_model.primary_metering_unit
    description: Primary metering unit for cost attribution and budgets.
    options:
    - option_id: token
      summary: Token-based metering (prompt+completion tokens).
      unit: token
    - option_id: request
      summary: Request-based metering (per call/run).
      unit: request
    - option_id: time
      summary: Time-based metering (compute seconds/minutes).
      unit: time
    - option_id: blended
      summary: Blended metering (tokens + requests + time) with weights.
      unit: blended
    - option_id: custom
      summary: Custom (fill fields).
      unit: ''
    default_option_id: token
  human_questions:
  - question_id: Q-CAF-COST-01-01
    topic: cost_model.primary_metering_unit
    question: Which metering unit will you adopt for budgets and cost attribution?
    status: open
    option_set_id: cost_model.primary_metering_unit
    answer: {}
