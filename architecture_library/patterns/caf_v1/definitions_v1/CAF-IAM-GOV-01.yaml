pattern_id: CAF-IAM-GOV-01
name: Centralized Identity Authority
summary: All platform-wide identity authority is centralized in the Control Plane.
intent: All platform-wide identity authority is centralized in the Control Plane.
context: See extended_definition (source section) for applicability constraints and assumptions.
problem: See extended_definition (source section) for the motivating problem and risks.
forces_tradeoffs: See extended_definition (source section) for trade-offs and constraints.
solution_structure_behavior: See extended_definition (source section) for required structure and behavior.
consequences: See extended_definition (source section) for benefits, liabilities, and side effects.
implementation_notes: See extended_definition (source section) for implementation guidance and guardrails.
examples_known_uses: Not specified in source section.
related_patterns: |
  depends_on: CTX-01
  depends_on: OBS-01
  depends_on: POL-01
  refines: CAF-IAM-01
extended_definition: '### Centralized Identity Authority Pattern


  All platform-wide identity authority is centralized in the Control Plane.


  Under this pattern:


  - Identity principal types are defined and versioned centrally

  - Trust relationships and integration boundaries are governed explicitly

  - Identity lifecycle events (creation, suspension, revocation) are managed centrally

  - Identity policy intent is authored and stored in the Control Plane


  Downstream systems may **consume identity authority**, but must not redefine it.


  ---'
source_documents:
- path: architecture_library/22_contura_identity_access_pattern_guide_v1.md
  section_heading: Centralized Identity Authority Pattern
caf:
  layer: caf
  verifiability: plan_ready
  plane: control
  allowed_anchor_types:
  - checklist_item
  - caf_pattern
  - core_pattern
  - tbp
  - rail
  default_anchor_policy: requires_explicit_design_decision
  default_anchor:
    anchor_type: checklist_item
    anchor_id: REQUIRES_HUMAN_DECISION_ID
  minimum_evidence:
  - '- Identity principal types are defined and versioned centrally - Trust relationships and integration boundaries are governed
    explicitly - Identity lifecycle events (creation, suspension, revocation) are managed centrally - Identity policy intent
    is authored and stored in the Control Plane Downstream systems may **consume identity authority**, but must not redefine
    it.'
  definition_of_done:
  - Verify that - Identity principal types are defined and versioned centrally - Trust relationships and integration boundaries
    are governed explicitly - Identity lifecycle events (creation, suspension, revocation) are managed centrally - Identity
    policy intent is authored and stored in the Control Plane Downstream systems may **consume identity authority**, but must
    not redefine it.
  capabilities_needed:
  - identity_authentication
  - policy_enforcement
  - revocation_propagation
  forbidden_assumptions:
  - No cloud vendor or product selection implied by this pattern.
  - No assumption of multi-tenancy unless explicitly stated by the spec/design.
  - No assumption of multi-region or global deployment unless explicitly stated by the spec/design.
  kind: decision_pattern
  option_sets:
  - option_set_id: iam.identity_authority_model
    description: How identity authority is organized and governed (control plane posture).
    options:
    - option_id: single_central_authority
      summary: Single centralized identity authority governs principals and trust rules.
      authority_kind: central
      notes: ''
    - option_id: federated_authorities
      summary: Federated authorities with explicit trust relationships and boundary checks.
      authority_kind: federated
      notes: ''
    - option_id: delegated_tenant_authority
      summary: Tenant-delegated identity authority within CP-governed constraints.
      authority_kind: delegated_tenant
      notes: ''
    - option_id: custom
      summary: Custom (fill fields).
      authority_kind: ''
      notes: ''
    default_option_id: single_central_authority
  human_questions:
  - question_key: identity_authority
    question_id: Q-IAM-AUTHORITY-01
    question: What identity authority model applies (central, federated, delegated tenant)?
    description: Select how identity authority is structured and governed.
    option_set_id: iam.identity_authority_model
    specialization_notes:
    - Warranted when multiple principal types or tenant-scoped identities exist.
    - Ensure the authority model supports auditability and revocation propagation.
    anchors:
    - caf_pattern: CAF-IAM-GOV-01
